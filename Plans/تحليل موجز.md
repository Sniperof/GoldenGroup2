\*\*تحليل موجز\*\*

وضعت `ScopeType` و`AuthContext` ونتائج `authorize()` في طبقة مشتركة داخل \[packages/shared/types/authorization.ts](D:/OneDrive/سطح المكتب/golden-crm-clean/packages/shared/types/authorization.ts:1) حتى تبقى canonical بين REST و`tRPC`. ووضعت `buildAuthContext()` و`authorize()` و`resolveActingBranch()` في \[packages/api/services/authorizationService.ts](D:/OneDrive/سطح المكتب/golden-crm-clean/packages/api/services/authorizationService.ts:1) لأنها خدمة تطبيقية مركزية وليست منطق transport.



أقل تغييرات لازمة كانت ربط هذه الطبقة فقط مع نقاط الدخول الحالية: \[packages/api/middleware/auth.ts](D:/OneDrive/سطح المكتب/golden-crm-clean/packages/api/middleware/auth.ts:1) لبناء `AuthContext` عند المصادقة، \[packages/api/middleware/permission.ts](D:/OneDrive/سطح المكتب/golden-crm-clean/packages/api/middleware/permission.ts:1) لاستهلاك `authorize()` بدل فحص permissions بشكل متناثر، و\[packages/api/trpc/init.ts](D:/OneDrive/سطح المكتب/golden-crm-clean/packages/api/trpc/init.ts:1) لتوحيد السياق والمنطق نفسه في `tRPC`. كذلك صدّرت الأنواع الجديدة من \[packages/shared/index.ts](D:/OneDrive/سطح المكتب/golden-crm-clean/packages/shared/index.ts:1). لم أقم بأي refactor واسع للمسارات.



\*\*التنفيذ\*\*

أضفت تعريفاً رسمياً واحداً للـ scopes: `GLOBAL | BRANCH | ASSIGNED`، وأضفت `PermissionGrant`, `AuthContext`, `AuthorizationCheck`, `AuthorizationResult`. ثم بنيت خدمة authorization أولية تقوم بـ:

\- `resolveActingBranch()` لتوحيد حل `X-Branch-Id`

\- `buildAuthContext()` لتجهيز `userId`, `roleId`, `isSuperAdmin`, `grants`, `allowedBranchIds`, `actingBranchId`

\- `authorize()` كنقطة دخول موحدة لفحص permission وscope



بعدها ربطت `requireAuth` و`requirePermission` و`tRPC createContext/withPermission` بهذه الخدمة الجديدة، بحيث صار لدينا foundation layer واحدة مشتركة بدون تغيير شامل للـ endpoints. تحققت أيضاً من البناء عبر `tsc` مباشرة من `node\_modules`, ونجح typecheck للـ API والويب.



\*\*ما بعد التنفيذ\*\*

الملفات المعدلة:

\- \[packages/shared/types/authorization.ts](D:/OneDrive/سطح المكتب/golden-crm-clean/packages/shared/types/authorization.ts:1)

\- \[packages/api/services/authorizationService.ts](D:/OneDrive/سطح المكتب/golden-crm-clean/packages/api/services/authorizationService.ts:1)

\- \[packages/api/middleware/auth.ts](D:/OneDrive/سطح المكتب/golden-crm-clean/packages/api/middleware/auth.ts:1)

\- \[packages/api/middleware/permission.ts](D:/OneDrive/سطح المكتب/golden-crm-clean/packages/api/middleware/permission.ts:1)

\- \[packages/api/trpc/init.ts](D:/OneDrive/سطح المكتب/golden-crm-clean/packages/api/trpc/init.ts:1)

\- \[packages/shared/index.ts](D:/OneDrive/سطح المكتب/golden-crm-clean/packages/shared/index.ts:1)

\- \[packages/shared/types/auth.ts](D:/OneDrive/سطح المكتب/golden-crm-clean/packages/shared/types/auth.ts:1) لم يتغير منطقياً، لكنه ظهر ضمن تعديلات الملف أثناء التحديث.



ما أصبح جاهزاً الآن:

\- تعريف رسمي واحد للـ scopes

\- `AuthContext` موحد

\- `buildAuthContext()` و`authorize()` كنقطة دخول مركزية

\- `resolveActingBranch()` موحد ومستخدم في Express و`tRPC`

\- عدم إضافة أي role-name checks جديدة أو permission names تحمل scope



ما بقي للمرحلة الثانية:

\- نقل المسارات الحساسة تدريجياً لاستخدام `authorize()` بشكل أوسع

\- استبدال الاعتماد الفعلي على grants الافتراضية بنموذج grants رسمي من قاعدة البيانات

\- التخلص المرحلي من legacy role-name checks الموجودة مسبقاً مثل `requireRole(...)`

\- تمهيد branch assignments الرسمية بدل fallback الحالي



الافتراضات المؤقتة التي أضفتها:

\- كل permission محملة حالياً من `role\_permissions` تُحوَّل مؤقتاً إلى grant افتراضي من نوع `GLOBAL` داخل `buildAuthContext()`

\- `allowedBranchIds` ما زالت تُستنتج محافظاً من `user.branchId` الحالي فقط

\- `actingBranchId` ما زال يعتمد على `X-Branch-Id` للمشرف العام مع الحفاظ على السلوك الحالي



نعم، ما زال هناك اعتماد على legacy role logic في أماكن لم ألمسها، وأبرزها `requireRole(...)` نفسه وبعض المسارات القديمة التي تستخدمه. لم أوسّع النطاق لإزالتها الآن، لكني لم أضف أي اعتماد جديد عليها.

