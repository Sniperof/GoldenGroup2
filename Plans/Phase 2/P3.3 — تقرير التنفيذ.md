  P3.3 — تقرير التنفيذ

  ---
  1) اسم الملف المضاف

  migrations/033_customer_service_supervisor_role_seed.sql
  226 سطر

  ---
  2) ما الذي يضمنه

  ┌─────────────────────────────────────────────────────────────────┬────────────────────────────────┐
  │                             الضمان                              │            التفصيل             │
  ├─────────────────────────────────────────────────────────────────┼────────────────────────────────┤
  │ CUSTOMER_SERVICE_SUPERVISOR template موجود في كل environment    │ DO block بـ IF NOT EXISTS      │
  ├─────────────────────────────────────────────────────────────────┼────────────────────────────────┤
  │ 10 grants بـ scopes صحيحة في role_permission_grants             │ ON CONFLICT DO UPDATE          │
  ├─────────────────────────────────────────────────────────────────┼────────────────────────────────┤
  │ Legacy compatibility في role_permissions                        │ ON CONFLICT DO NOTHING         │
  ├─────────────────────────────────────────────────────────────────┼────────────────────────────────┤
  │ SYSTEM_ADMIN يحتفظ بكل clients.* بـ GLOBAL حتى بعد 025          │ INSERT + ON CONFLICT DO UPDATE │
  ├─────────────────────────────────────────────────────────────────┼────────────────────────────────┤
  │ HR_ASSISTANT يحتفظ بـ clients.view_list وclients.view بـ BRANCH │ INSERT + ON CONFLICT DO UPDATE │
  ├─────────────────────────────────────────────────────────────────┼────────────────────────────────┤
  │ لا حذف لأي grants موجودة                                        │ لا يوجد DELETE في الملف        │
  ├─────────────────────────────────────────────────────────────────┼────────────────────────────────┤
  │ لا user changes                                                 │ لا يوجد UPDATE على hr_users    │
  ├─────────────────────────────────────────────────────────────────┼────────────────────────────────┤
  │ لا clone creation                                               │ branch_id = NULL دائماً        │
  └─────────────────────────────────────────────────────────────────┴────────────────────────────────┘

  ---
  3) هل هو Idempotent؟

  نعم — مثبت بثلاثة اختبارات:

  ┌──────────────────────────────────────────────┬────────────────────────────────┐
  │                   الاختبار                   │            النتيجة             │
  ├──────────────────────────────────────────────┼────────────────────────────────┤
  │ تشغيل مرة واحدة في ROLLBACK                  │ ✅ EXIT 0                      │
  ├──────────────────────────────────────────────┼────────────────────────────────┤
  │ تشغيل مرتين متتاليتين في نفس الـ transaction │ ✅ EXIT 0 — نفس النتائج بالضبط │
  ├──────────────────────────────────────────────┼────────────────────────────────┤
  │ تشغيل حقيقي على production                   │ ✅ EXIT 0                      │
  └──────────────────────────────────────────────┴────────────────────────────────┘

  الـ INSERT 0 0 في النتائج يؤكد أن الـ ON CONFLICT paths تعمل صح — لا تكرار.

  ---
  4) كيف يتصرف على Production؟

  DO block      → role موجود (id=39) → no-op ✅
  CSS grants    → كلها موجودة → ON CONFLICT DO UPDATE (scope_type يُثبَّت) ✅
  CSS legacy    → كلها موجودة → ON CONFLICT DO NOTHING ✅
  SYSTEM_ADMIN  → grants موجودة (025 لم تُطبَّق) → ON CONFLICT DO UPDATE ✅
  HR_ASSISTANT  → grants موجودة → ON CONFLICT DO UPDATE ✅

  لا تغيير فعلي على production — migration مطبقة ومسجّلة كـ idempotent run.                                                                         ─

  ---
  5) كيف يتصرف على Staging / Fresh Env؟

  سيناريو Staging (017–032 ثم 033):

  DO block      → role غير موجود → INSERT new role ✅
  CSS grants    → table فارغ → INSERT 10 grants ✅
  SYSTEM_ADMIN  → 025 حذفت grants → ON CONFLICT DO UPDATE يُعيدها ✅
  HR_ASSISTANT  → 025 حذفت grants → ON CONFLICT DO UPDATE يُعيد view* ✅                                                                            ─

  سيناريو Fresh Env (000→033):

  نفس سيناريو staging بالضبط — CSS role يُنشأ من الصفر

  ---
  6) هل يصلح أثر Migration 025؟

  نعم — مثبت بالاختبار الكامل:

  محاكاة 025 (DELETE 59 grants من كل clients.*)
    ↓
  CSS clients grants بعد 025:   0  ← مكسور
  SYSTEM_ADMIN clients grants:  0  ← مكسور

  بعد تطبيق 033:
    ↓
  CSS clients grants:   5 grants (view_list/view/view_detail/create/edit) ✅
  SYSTEM_ADMIN clients: 6 grants (كل clients.* بـ GLOBAL) ✅
  HR_ASSISTANT clients: 2 grants (view_list/view بـ BRANCH) ✅

  ---
  7) هل يحتاج تطبيقه على Production الآن؟

  نعم — طُبّق فعلاً.

  applied_at: 2026-04-25 11:16:20.870658+00
  registered: schema_migrations ✅

  كان هو نفسه no-op على production (الدور والgrants موجودة)، لكن تسجيله ضروري لمنع الـ runner من تطبيقه مستقبلاً.                                   ─

  ---
  8) SQL Syntax Check

  ✅ psql --ON_ERROR_STOP=1 → EXIT 0
  ✅ تشغيل كامل في transaction → لا أخطاء
  ✅ تشغيل حقيقي على production → لا أخطاء
  ✅ لا hardcoded IDs
  ✅ لا DDL على جداول موجودة
  ✅ كل INSERT مع ON CONFLICT

  ---
  ملاحظة: HR_ASSISTANT grants الإضافية

  HR_ASSISTANT يظهر في التحقق بـ 5 grants للـ clients (بما فيها create/edit/delete) — هذه موجودة من migration 024 التي منحت كل templates كل
  permissions. migration 033 أضافت/ثبّتت view_list وview فقط (كما هو مطلوب) — الباقي موجود مسبقاً ولم تلمسه.                                        ──

  ---
  معايير القبول — التحقق النهائي

  ┌────────────────────────────────────────────────────┬───────────────────────────────┐
  │                      المعيار                       │            الحالة             │
  ├────────────────────────────────────────────────────┼───────────────────────────────┤
  │ CUSTOMER_SERVICE_SUPERVISOR قابل للإعادة من الريبو │ ✅ migration 033 في الريبو    │
  ├────────────────────────────────────────────────────┼───────────────────────────────┤
  │ Grants موجودة في role_permission_grants            │ ✅ 10 grants بـ scopes صحيحة  │
  ├────────────────────────────────────────────────────┼───────────────────────────────┤
  │ SYSTEM_ADMIN لا يفقد clients حتى لو 025 طُبّقت     │ ✅ مثبت بالاختبار            │  │
  ├────────────────────────────────────────────────────┼───────────────────────────────┤
  │ آمنة على production (الدور الموجود لا يُكسر)       │ ✅ no-op على production      │ │
  ├────────────────────────────────────────────────────┼───────────────────────────────┤
  │ لا user changes                                    │ ✅ samia.role_id=39 لم يتغيّر │
  ├────────────────────────────────────────────────────┼───────────────────────────────┤
  │ لا clone creation                                  │ ✅ branch_id=NULL             │
  ├────────────────────────────────────────────────────┼───────────────────────────────┤
  │ Idempotent                                         │ ✅ مثبت بثلاثة اختبارات       │
  └────────────────────────────────────────────────────┴───────────────────────────────┘