# نظام الهدايا — `gift_records`

> **الحالة:** معتمد دستوريا — آخر تسوية منتج 2026-07-26، والتنفيذ البرمجي قيد المطابقة
> **النطاق:** V1 لنظام هدايا العقود ووسطاء البيعة، بدون مخزون وبدون محرك استحقاق آلي
> **العلاقة بالمهام:** مهمة التسليم المرجعية هي [`gift_delivery`](./tasks/gift-delivery.md)

---

## مدخل مفاهيمي

الهدية في Golden CRM ليست مهمة تسليم بحد ذاتها. الهدية هي سجل تشغيلي قابل للتتبع يبدأ كوعد، ثم قد يعتمد للتسليم، ثم ينفذ عبر مهمة `gift_delivery` أو عبر تأكيد تسليم يدوي في حالات محددة.

القرار الحاكم:

> **`gift_records` هو مصدر الحقيقة للوعد والاستحقاق والاعتماد، أما `gift_delivery` فهي مسار التنفيذ الميداني عند الحاجة فقط.**

هذا يعني:
- لا تنشأ مهمة تسليم لمجرد وجود وعد هدية.
- لا يتوجب أن يكون الشرط محققا حتى تنشأ مهمة التسليم.
- اعتماد سجل الهدية للتسليم يجمد الكمية.
- كل وعد قرار مستقل وسجل مستقل؛ لا يدمج النظام الوعود المتشابهة تلقائيا.
- يمكن ربط أكثر من مصدر إثبات بوعد موجود يدويا، لكن ذلك لا يضيف كمية ولا يغير الاستحقاق.
- لا يوجد تسليم جزئي في V1.

---

## أ — تعريف الهدية

### `gift_definitions`

`gift_definitions` هو كتالوج إداري بسيط لأنواع الهدايا الممكن منحها.

هذا الكتالوج لا يمثل مخزونا، ولا يدير رصيدا، ولا يحجز موادا.

الحقول المفهومية:

| الحقل | القاعدة |
|---|---|
| `id` | معرف داخلي |
| `name` | اسم الهدية المعروض |
| `description` | وصف اختياري |
| `default_unit_label` | تسمية وحدة العرض مثل: قطعة، طقم، خدمة |
| `is_active` | هل التعريف متاح للاستخدام |
| `created_at` / `updated_at` | تتبع إداري |

القواعد:
- لا يجوز إنشاء سجل هدية جديد على `gift_definition` غير فعال إلا بصلاحية تصحيح إدارية لاحقة إن تقررت.
- تعطيل تعريف الهدية لا يغير السجلات التاريخية.
- لا يحمل تعريف الهدية أي متطلبات توثيق خاصة. توثيق التسليم موحد عبر نتيجة `gift_delivery` أو تأكيد يدوي.

---

## ب — سجل الهدية

### `gift_records`

`gift_records` هو الكيان الرئيسي للنظام. الاسم محايد لأنه يغطي الوعد، تحقق الشرط، الاعتماد، التسليم، والتأكيد اليدوي.

### المستفيد

سجل الهدية يجب أن يحدد المستفيد بوضوح.

القيم المفهومية:

```ts
type GiftBeneficiaryType =
  | 'contract_customer'
  | 'customer_referrer'
  | 'employee_referrer'
  | 'personal_referrer';
```

القواعد:
- `contract_customer`: الزبون صاحب العقد.
- `customer_referrer`: وسيط البيعة من نوع زبون، ويرتبط بـ `beneficiary_client_id`.
- `employee_referrer`: وسيط البيعة من نوع موظف، ويرتبط بـ `beneficiary_employee_id`.
- `personal_referrer`: وسيط شخصي معروف الهوية لكنه ليس زبونا ولا موظفا في السجل الوظيفي.
- لا تقبل قيمة `Unknown` ولا أي نوع خارج القائمة المغلقة.
- نوع المستفيد مستقل عن نوع المصدر؛ فقد يأتي وعد وسيط الزبون من عقد أو لائحة أسماء أو اقتراح مباشر.

### حالات السجل

```ts
type GiftRecordStatus =
  | 'promised'
  | 'approved_for_delivery'
  | 'delivery_task_created'
  | 'delivered'
  | 'delivered_manually'
  | 'cancelled'
  | 'refused';
```

| الحالة | المعنى |
|---|---|
| `promised` | وعد هدية قائم |
| `approved_for_delivery` | تم اعتماد السجل للتسليم وتجميد الكمية |
| `delivery_task_created` | تم إنشاء مهمة `gift_delivery` |
| `delivered` | تم التسليم عبر مهمة ميدانية |
| `delivered_manually` | تم تأكيد التسليم يدويا بدون مهمة |
| `cancelled` | ألغي السجل قبل إنشاء مهمة التسليم |
| `refused` | المستفيد رفض الهدية |

### حالة تحقق الشرط

تحقق الشرط رقابي وتشغيلي، لكنه ليس بوابة إلزامية لإنشاء مهمة التسليم.

```ts
type GiftConditionStatus =
  | 'pending'
  | 'met'
  | 'not_met';
```

القواعد:
- السجل يبدأ عادة بـ `condition_status='pending'`.
- لا يغير `condition_status` إلا فعل `verify_condition` بواسطة مستخدم مخول.
- كل تحقق يحفظ القرار والمستخدم والوقت والملاحظات.
- يمكن اعتماد السجل للتسليم حتى لو `condition_status='pending'` أو `condition_status='not_met'`.
- إذا تم الاعتماد بينما الشرط `not_met`، تصبح ملاحظات الاعتماد إلزامية.
- إضافة مستند أو مصدر إثبات لا تغير `condition_status` آليا.
- بعد حالة نهائية لا يعدل الشرط مباشرة.

### الحقول المفهومية الأساسية

| الحقل | القاعدة |
|---|---|
| `gift_definition_id` | تعريف الهدية |
| `beneficiary_type` | نوع المستفيد |
| `beneficiary_client_id` | مطلوب عندما يكون المستفيد زبونا |
| `beneficiary_employee_id` | مطلوب عندما يكون المستفيد موظفا |
| `beneficiary_label` | لقطة اسم للعرض والتاريخ |
| `condition_id` | من `system_lists` فئة `gift_promise_conditions` |
| `condition_status` | حالة تحقق الشرط |
| `status` | حالة مسار الهدية |
| `promised_quantity` | الكمية التي قررها منشئ الوعد عند الإنشاء |
| `approved_quantity` | `NULL` قبل الاعتماد، ثم الكمية المجمدة عند الاعتماد |
| `source_branch_id` | فرع مصدر الاستحقاق، غالبا فرع العقد |
| `responsible_branch_id` | فرع متابعة الهدية |
| `assigned_user_id` | المسؤول الإداري عن متابعة السجل |
| `condition_verified_by` / `condition_verified_at` | من حسم حالة الشرط ومتى |
| `approval_notes` | ملاحظات الاعتماد |
| `approved_by` / `approved_at` | من اعتمد الكمية ومتى |
| `condition_notes` | ملاحظات تحقق الشرط |
| `manual_delivery_method` | طريقة التنفيذ اليدوي المعتمدة |
| `manual_delivered_by` / `manual_delivered_at` | من أكد التنفيذ اليدوي ومتى |
| `manual_delivery_branch_id` | الفرع الذي تم فيه التسليم أو التنفيذ اليدوي |
| `manual_delivery_notes` | ملاحظات تأكيد التسليم اليدوي |

---

## ج — شروط الوعد

شروط أو أسباب وعد الهدية لا تنفذ آليا في V1. هي قيم إدارية يختارها المستخدم من `system_lists`.

الفئة المعتمدة:

```text
gift_promise_conditions
```

أمثلة قيم:
- `contract_referrer_gift`
- `cash_contract`
- `after_second_installment`
- `multiple_contracts`
- `administrative_commitment`
- `branch_manager_decision`
- `other`

القواعد:
- الشرط وصف إداري، وليس قاعدة آلية.
- عند اختيار `other` تصبح ملاحظة الشرط إلزامية.
- النظام لا يراقب الدفعات أو عدد العقود آليا في V1.
- القرار البشري هو مصدر الحقيقة لـ `condition_status`.

---

## د — مصادر السجل

### `gift_record_sources`

يحفظ `gift_record_sources` منشأ الوعد وأدلته المرجعية. وجود عدة مصادر لا يعني دمج عدة وعود ولا احتساب كمية تلقائية.

مثال: إذا كان لوسيط زبون وعد سابق من لائحة أسماء ثم ظهر كوسيط عقد، يجوز للمستخدم ربط العقد كمصدر إثبات إضافي بالوعد السابق. لا يغير هذا الربط `promised_quantity` أو `approved_quantity`.

القيم المفهومية لـ `source_type`:

```ts
type GiftRecordSourceType =
  | 'contract'
  | 'name_list'
  | 'direct_referral'
  | 'candidate';
```

الحقول المفهومية:

| الحقل | القاعدة |
|---|---|
| `gift_record_id` | السجل الرئيسي |
| `source_type` | عقد، لائحة أسماء، اقتراح مباشر |
| `contract_id` | عند المصدر العقدي |
| `referral_sheet_id` | عند المصدر من لائحة الأسماء |
| `direct_referral_id` | عند وجود كيان اقتراح مباشر لاحقا |
| `candidate_id` | عند إنشاء وعد من اسم مقترح مباشر محفوظ في `candidates` |
| `source_label` | لقطة نصية للعرض |
| `quantity` | لقطة اختيارية تاريخية، وليست محرك جمع أو استحقاق |
| `notes` | ملاحظات اختيارية |

قواعد أهلية مصادر الترشيح:
- `source_type='name_list'`: يجوز إنشاء وعد هدية من لائحة الأسماء فقط إذا كان وسيط اللائحة من نوع زبون/Client ومرتبطا بسجل زبون معروف. عندها يكون المستفيد هو وسيط اللائحة، وليس كل اسم داخل اللائحة.
- `source_type='direct_referral'`: يستخدم عندما يملك الاقتراح المباشر كيانا مستقلا قابلا للتتبع.
- `source_type='candidate'`: يستخدم عندما يكون الاقتراح المباشر محفوظا كسجل في `candidates` وليس في `direct_suggestions`.
- إذا أصبح نفس المستفيد وسيط بيعة ضمن عقد لاحق، يعرض النظام وعوده غير المحققة السابقة من كل المصادر قبل حفظ الوعد العقدي.
- وجود وعد سابق لا يمنع إنشاء وعد جديد؛ قرار الاستحقاق بشري في V1.
- يجوز بدلا من إنشاء وعد جديد ربط العقد يدويا كمصدر إثبات بوعد موجود، دون زيادة كمية أو تغيير حالة الشرط.
- حالة تحقق الشرط لكل من لائحة الأسماء والاقتراح المباشر تبقى يدوية بالكامل في V1، ولا يعتمد النظام على شراء الاسم المقترح آليا.

قواعد التكرار:
- لا يجوز إدخال نفس المصدر مرتين في نفس سجل الهدية لنفس الشرط.
- يسمح بوجود عدة `gift_records` لنفس المستفيد ونفس تعريف الهدية ونفس الشرط.
- قبل إنشاء وعد مشابه، يبحث النظام عن الوعود غير المحققة لنفس المستفيد، وبخاصة الوعود المنشأة من `name_list` أو `direct_referral` أو `candidate` أو عقد سابق.
- يعرض النظام تحذيرا غير مانع يوضح السجلات المتشابهة وحالاتها ومصادرها.
- إذا تابع المستخدم بعد التحذير، ينشأ سجل جديد مستقل ويسجل أن المستخدم رأى التحذير وتابع، مع معرف المستخدم والوقت ومعرفات السجلات المشابهة.
- لا دمج تلقائيا، ولا ربط تلقائيا، ولا تعديل تلقائي للكمية أو الشرط.

---

## هـ — إنشاء الوعد

إنشاء وعد الهدية يدوي بالكامل في V1.

نقاط الإنشاء:
- من تفاصيل العقد، لهدية تخص صاحب العقد.
- من لائحة الأسماء المقترحة أو سياق وسيط البيعة.
- من الاقتراح المباشر إذا كان نوع الوسيط زبونا أو موظفا.
- من الصفحة المركزية عند الحاجة، مع اختيار مصدر واضح.

### وعود العقد المسودة

- أثناء كون العقد مسودة، تحفظ وعوده في `draft_gift_promises` ولا تعد سجلات تشغيلية.
- لا يجوز لمسار تفاصيل عقد مسودة إنشاء `gift_record` حقيقي خارج عملية اعتماد العقد.
- عند اعتماد العقد، تحول كل الوعود المسودة إلى `gift_records` وتضاف مصادرها العقدية داخل معاملة اعتماد العقد نفسها.
- أي فشل في تحويل وعد واحد يفشل اعتماد العقد كاملا، ويرجع كامل المعاملة، وتبقى المسودات دون حذف.
- لا تفرغ `draft_gift_promises` إلا بعد نجاح إنشاء جميع السجلات والمصادر ضمن المعاملة.
- لا يجوز تجاوز خطأ التحويل بـ `console.warn` أو `SAVEPOINT` ثم متابعة اعتماد العقد.
- بعد أن يصبح العقد فعالا، يمكن إنشاء وعد جديد مباشرة كسجل `gift_record` مستقل.

لا يوجد:
- توليد وعد غير مدخل من المستخدم عند اعتماد العقد.
- إنشاء تلقائي عند دفع قسط.
- إنشاء تلقائي عند بلوغ عدد عقود.
- cron أو job لتوليد الهدايا.

---

## و — وسيط البيعة

### القواعد

- إذا كان وسيط البيعة من نوع زبون، يمكن إنشاء مهمة `gift_delivery` له.
- مهمة التسليم تنشأ على زبون الوسيط نفسه، لا على صاحب العقد.
- إذا كان الوسيط من نوع `personal_referrer` أو `employee_referrer` فلا تنشأ له مهمة ميدانية لأنه لا يملك `client_id` قابلا للتخطيط، ويمكن تنفيذ الهدية يدويا.
- يمكن أيضا تنفيذ هدية زبون يدويا عندما يستلمها في الشركة أو يكون محل الهدية عقدا أو منفعة لا تحتاج زيارة.
- `Unknown` لا يصح كوسيط بيعة في عقود جديدة.

### أثر ذلك على `gift_records`

| نوع الوسيط | وعد هدية | مهمة تسليم | تأكيد يدوي |
|---|:---:|:---:|:---:|
| زبون | نعم | نعم | نعم، إذا كان التنفيذ دون زيارة |
| موظف | نعم | لا | نعم |
| شخصي | نعم | لا | نعم |
| مجهول | مرفوض في العقود الجديدة | لا | لا |

---

## ز — الاعتماد والتسليم

### الاعتماد للتسليم

اعتماد السجل للتسليم:
- مسموح فقط من `status='promised'`.
- يغير `status` إلى `approved_for_delivery`.
- يثبت `approved_quantity` صراحة، ولا يعتمد على جمع المصادر.
- يحفظ `approved_by`, `approved_at`, و`approval_notes`.
- يمنع أي تعديل على الكمية؛ ويجوز إلحاق مصدر إثبات لاحق يدويا دون أثر كمي أو شرطي.
- لا ينشئ مهمة تسليم تلقائيا.
- لا يسمح بتكرار فعل الاعتماد لتعديل الكمية.
- يجوز سحب الاعتماد قبل إنشاء مهمة، بصلاحية `contract_gifts.approve_delivery` وسبب إلزامي، فيعود السجل إلى `promised`.
- بعد إنشاء مهمة، لا تستخدم عملية سحب الاعتماد؛ يعالج التصحيح عبر مسار المهمة والزيارة.

### إنشاء مهمة التسليم

بعد الاعتماد، يظهر إجراء مستقل لإنشاء مهمة `gift_delivery`.

الشروط:
- كل السجلات المختارة بحالة `approved_for_delivery`.
- كل السجلات لنفس `beneficiary_client_id` ونفس `responsible_branch_id`.
- المستفيد زبون قابل للتخطيط.
- لا توجد مهمة `gift_delivery` نشطة لأي سجل مختار.
- يختار المستخدم السجلات صراحة؛ لا يجمعها النظام تلقائيا.
- وجود `due_date`, `priority`, وملاحظات اختيارية.
- تحفظ العلاقة في جدول ربط صريح بين المهمة وكل `gift_record`، ولا يمثلها `source_context_id` لسجل أول.
- نتيجة المهمة جماعية وغير جزئية: النجاح أو الرفض أو إعادة الجدولة ينعكس على جميع السجلات المرتبطة.
- يجب أن تحذر الواجهة قبل الإنشاء بأن التجميع يعني نتيجة واحدة لجميع السجلات.

### التأكيد اليدوي

يستخدم `delivered_manually` عندما يتم التسليم أو تنفيذ المنفعة دون زيارة ميدانية، بصرف النظر عن نوع المستفيد. أمثلة ذلك: استلام الزبون من مقر الشركة، تسليم مباشر، أو هدية من نوع عقد/منفعة.

القواعد:
- السجل بحالة `approved_for_delivery`.
- لا توجد مهمة تسليم نشطة مرتبطة به.
- التأكيد اليدوي يتطلب صلاحية مخصصة.
- يجب اختيار طريقة من قائمة `gift_manual_delivery_methods`، مثل:
  - `company_pickup`
  - `direct_handover`
  - `contract_handover`
  - `other`
- يجب حفظ من أكد التسليم ومتى وفرع التنفيذ وملاحظات مختصرة.
- يجب وجود إقرار صريح بأن كامل الكمية المعتمدة سلمت أو نفذت.
- التأكيد اليدوي يغلق كامل الكمية المعتمدة.
- لا يلزم توقيع أو صورة أو مرفق.

---

## ح — الإلغاء والرفض

### الإلغاء

يمكن إلغاء سجل الهدية فقط قبل إنشاء مهمة التسليم.

الحالات المسموحة:
- `promised`
- `approved_for_delivery`

بعد `delivery_task_created` لا يلغى سجل الهدية مباشرة. يعالج المسار عبر مهمة التسليم ونتيجتها.

### إلغاء مهمة التسليم قبل زيارة فعالة

يستخدم مسار إلغاء المهام العام عندما تكون حالة المهمة قابلة للإلغاء ولا توجد زيارة فعالة.

- تلغى محاولة التنفيذ الميداني فقط، ولا يلغى وعد الهدية ولا يسجل رفضا على المستفيد.
- تعود كل سجلات الهدية المرتبطة بالمهمة إلى `approved_for_delivery`.
- تزال الرابطة النشطة التي تمنع إنشاء مهمة جديدة، مع بقاء علاقة المهمة الملغاة وأثرها التاريخي.
- يستخدم سبب من `gift_delivery_task_cancellation_reasons`، ولا تستخدم `gift_delivery_refusal_reasons`.
- إذا كانت المهمة `needs_follow_up` بعد إعادة جدولة سابقة، تبقى الزيارات السابقة ونتائجها تاريخية، وتعود السجلات إلى `approved_for_delivery` ما دام لا يوجد رفض فعلي.
- إذا وجدت زيارة فعالة، يمنع هذا الإلغاء ويجب إنهاء المسار من داخل الزيارة.

### الرفض وإعادة الجدولة

إذا رفض المستفيد الهدية في نتيجة مهمة التسليم:
- تصبح مهمة التسليم منتهية.
- يصبح `gift_records.status='refused'`.
- لا يغير هذا القرار من صفحة سجل الهدية؛ أي تصحيح يمر عبر إعادة فتح الزيارة.

إذا سجلت نتيجة مهمة التسليم كـ `rescheduled`:
- تبقى نفس مهمة `gift_delivery` حية للمتابعة.
- يبقى `gift_records.status='delivery_task_created'`.
- يجب تسجيل سبب من `gift_delivery_reschedule_reasons`.
- يجب تسجيل تاريخ متابعة، دون وقت.

### تصحيح نتيجة نهائية

لا يوجد مسار تصحيح مستقل خاص بالهدايا عندما يكون التسليم منفذا عبر مهمة:

1. إذا كانت الزيارة `closed`، تعاد إلى `ended` بصلاحية `field_visits.reopen_closed` وسبب إلزامي.
2. تعدل نتيجة نفس `visit_task` من داخل الزيارة عبر مسار النتائج الموحد.
3. يعاد انعكاس النتيجة جماعيا على `open_task` وكل سجلات الهدايا المرتبطة داخل معاملة واحدة.
4. تستكمل الزيارة وتقفل مجددا.

لا ينشأ سجل هدية أو مهمة جديدة بسبب التصحيح، ولا يسمح بتعديل `delivered` أو `refused` مباشرة من صفحة الهدايا.

أما `delivered_manually` فلا يملك زيارة. يعاد فتحه بفعل إداري مماثل يتطلب `contract_gifts.reopen_manual_delivery` وسببا إلزاميا، فيعود إلى `approved_for_delivery` مع الاحتفاظ بأثر التسليم السابق، ثم يمكن تأكيده يدويا مجددا أو إنشاء مهمة.

---

## ط — الفروع والإسناد

كل سجل هدية يحمل فرعين:

| الحقل | المعنى |
|---|---|
| `source_branch_id` | فرع مصدر الهدية، غالبا فرع العقد |
| `responsible_branch_id` | الفرع المسؤول عن متابعة الهدية |

الافتراضي:

```text
responsible_branch_id = source_branch_id
```

لكن يمكن تغييره بصلاحية عند الحاجة.

الإسناد مزدوج:
- `gift_records.assigned_user_id`: مسؤول إداري عن متابعة الوعد والتحقق والاعتماد.
- مهمة `gift_delivery`: لها إسناد وجدولة ميدانية مستقلة ضمن نظام المهام والزيارات.

---

## ي — الصلاحيات

الصلاحيات المقترحة:

| الصلاحية | الغرض | النطاقات المتوقعة |
|---|---|---|
| `gift_definitions.view` | عرض تعريفات الهدايا | `GLOBAL`, `BRANCH` |
| `gift_definitions.manage` | إدارة تعريفات الهدايا | غالبا `GLOBAL` |
| `contract_gifts.view` | عرض سجلات الهدايا | `GLOBAL`, `BRANCH`, `ASSIGNED` |
| `contract_gifts.manage` | إنشاء وتعديل سجلات الوعد قبل الإغلاق | `GLOBAL`, `BRANCH`, `ASSIGNED` |
| `contract_gifts.verify_condition` | تحديث حالة تحقق الشرط | `GLOBAL`, `BRANCH`, `ASSIGNED` |
| `contract_gifts.approve_delivery` | اعتماد السجل للتسليم | `GLOBAL`, `BRANCH`, `ASSIGNED` |
| `contract_gifts.create_delivery_task` | إنشاء مهمة `gift_delivery` | `GLOBAL`, `BRANCH`, `ASSIGNED` |
| `contract_gifts.manual_delivery` | تأكيد تسليم يدوي | `GLOBAL`, `BRANCH`, `ASSIGNED` |
| `contract_gifts.reopen_manual_delivery` | إعادة فتح تسليم يدوي نهائي بسبب موثق | `GLOBAL` فقط، مماثلة لفتح زيارة مقفلة |
| `contract_gifts.cancel` | إلغاء السجل قبل إنشاء مهمة | `GLOBAL`, `BRANCH`, `ASSIGNED` |
| `tasks.gifts.view` | عرض جدول مهام تسليم الهدايا | موجودة حاليا |

قواعد الإنفاذ:
- كل عملية على سجل هدية يجب أن تستخدم `identity + permission + scope + subject`.
- subject لسجل الهدية يتضمن `responsible_branch_id` و`assigned_user_id`.
- قوائم الصفحة المركزية يجب أن تطبق النطاق في query.
- الواجهة تخفي الأزرار فقط كتحسين UX، وليس كحماية.
- أي تنفيذ فعلي لهذه الصلاحيات يتطلب migration وتحديث `صلاحيات_النظام.xlsx`.
- نتائج المهام وتصحيحها لا تستخدم صلاحيات الهدايا بديلا عن صلاحيات الزيارات؛ تخضع للمسار الموحد وإعادة فتح الزيارة.

---

## ك — الواجهات المرجعية

### تفاصيل العقد

يعرض وعود هدايا تخص صاحب العقد ووسيط البيعة ومصادرها العقدية. في العقد المسودة يعرض `draft_gift_promises` فقط، وبعد الاعتماد يعرض `gift_records` المرتبطة فعليا.

### لائحة الأسماء المقترحة / الاقتراح المباشر

يعرض وعود هدايا تخص وسيط البيعة أو المقترح، حسب نوعه.

قبل حفظ وعد جديد، تعرض الواجهة تنبيها غير مانع عن الوعود غير المحققة المشابهة من جميع المصادر. يستطيع المستخدم المتابعة لإنشاء سجل مستقل أو اختيار ربط المصدر يدويا بوعد موجود دون تعديل الكمية.

### تفاصيل الزبون

تبويب "الهدايا" يعرض:
- الوعود.
- حالة تحقق الشرط.
- المعتمد للتسليم.
- مهام التسليم.
- ما تم استلامه.
- طريقة التنفيذ اليدوي أو نتيجة الزيارة.
- المصادر المرتبطة والتحذيرات التي تم تجاوزها.

### الصفحة المركزية

صفحة "إدارة الهدايا" تعرض:
- كل الفروع عند منحة `GLOBAL`.
- فرع محدد حسب `responsible_branch_id`.
- المسند لي حسب `assigned_user_id`.
- فلاتر الحالة، حالة الشرط، تعريف الهدية، المستفيد، المصدر.

---

## ل — قائمة فحص الإصدار

- [ ] إنشاء `gift_definitions`.
- [ ] إنشاء `gift_records`.
- [ ] إنشاء `gift_record_sources`.
- [ ] إضافة فئة `gift_promise_conditions` إلى `system_lists`.
- [ ] إضافة `gift_manual_delivery_methods` و`gift_delivery_task_cancellation_reasons` إلى `system_lists`.
- [ ] إضافة الصلاحيات المقترحة وتحديث `صلاحيات_النظام.xlsx`.
- [ ] policy لسجلات الهدايا يعتمد `responsible_branch_id` و`assigned_user_id`.
- [ ] صفحة تعريفات الهدايا.
- [ ] صفحة مركزية لإدارة الهدايا.
- [ ] تبويب الهدايا في تفاصيل الزبون.
- [ ] تبويب الزبون والصفحة المركزية يقرآن من `gift_records` الحقيقي ولا يعرضان سجلات mock/prototype.
- [ ] منتقيات تعريف الهدية تقرأ من `gift_definitions` الحقيقي ولا تستخدم قائمة prototype بعد ربط API.
- [ ] قسم هدايا في تفاصيل العقد.
- [ ] ربط إنشاء مهمة `gift_delivery` بسجل هدية معتمد.
- [ ] دعم تجميع عدة سجلات لنفس المستفيد والفرع في مهمة واحدة بعلاقة ربط صريحة.
- [ ] دعم التأكيد اليدوي لكل أنواع المستفيدين عندما يكون التنفيذ دون زيارة.
- [ ] دعم إعادة فتح التسليم اليدوي بصلاحية وسبب وأثر تدقيق.
- [ ] تنبيه غير مانع عن الوعود غير المحققة المشابهة وتسجيل قرار المتابعة.
- [ ] تحويل `draft_gift_promises` ذريّا عند اعتماد العقد ومنع حذفها عند فشل التحويل.
- [ ] إلغاء مهمة قبل زيارة يعيد السجلات إلى `approved_for_delivery` ولا يسجل `refused`.
- [ ] تصحيح نتيجة مهمة الهدية يمر عبر إعادة فتح الزيارة ومسار النتائج الموحد.
- [ ] منع `Unknown` كوسيط بيعة في العقود الجديدة.
- [ ] اختبارات سلبية: خارج الفرع، غير مسند، غياب الصلاحية، سجل ملغى، مهمة نشطة موجودة، تجميع مستفيدين أو فرعين مختلفين.
- [ ] اختبارات إيجابية: وعد عقد، وعد وسيط زبون، تحذير تكرار مع متابعة، اعتماد، مهمة مجمعة، إلغاء قبل زيارة، تصحيح بعد إعادة فتح، وتسليم يدوي لزبون.

---

## المراجع

- [مهمة تسليم الهدية](./tasks/gift-delivery.md)
- [معيار هندسة الصلاحيات](../domains/permissions-engineering-standard.md)
- [Open Tasks Domain](../domains/open-tasks.md)
- [Task Scheduling Patterns](../../analysis/task-scheduling-patterns.md)
- [Unified Task Model](../../analysis/unified-task-model.md)

---

## قرار مكمّل — عقد الهدية وإقرار التسليم

عقد الهدية الموجود في مسار العقود، أي العقد الذي يحمل `saleSubtype='free'`، يدخل ضمن نظام الهدايا عبر `gift_definitions` ولا يحتاج كياناً منفصلاً.

```ts
type GiftDefinitionKind =
  | 'standard_gift'
  | 'gift_contract';
```

القواعد:
- تعريف "عقد هدية / تمليك بلا مقابل" يكون `kind='gift_contract'`.
- عند إنشاء `gift_record` من عقد هدية، يكون المصدر `gift_record_sources.source_type='contract'` مع ربط `contract_id`.
- شرط الوعد/الاستحقاق المناسب يكون مثل `gift_contract`.
- تعريف الهدية لا يحمل مستلزمات تسليم خاصة.
- المطلوب الوحيد عند نجاح التسليم هو إقرار الزبون بالاستلام.
- لا يجوز حذف تعريف `gift_contract` حذفا صلبا؛ يمكن فقط إلغاء تفعيله.
- لا يجوز حذف أي `gift_definition` لديه وعد أو سجل استحقاق أو تسليم مرتبط. الإجراء المسموح هو `is_active=false` للحفاظ على التاريخ.

## قرار مكمّل — ملكية المستفيد و"المسندة لي"

فلتر "المسندة لي" في إدارة الهدايا لا يعتمد على إسناد يدوي مستقل داخل `gift_records`.

القاعدة:
- إذا كان المستفيد زبوناً، فإن ظهور سجل الهدية ضمن "المسندة لي" يتبع ملكية الزبون المستفيد نفسها.
- إذا كانت ملكية الزبون تعود لمشرفة أو أكثر، يظهر السجل لمن يملك صلاحية رؤية هذا الزبون ضمن نفس سياسة الزبائن.
- إذا كانت ملكية الزبون على مستوى الفرع، يظهر السجل ضمن صلاحيات الفرع كما يظهر الزبون نفسه.
- `responsible_branch_id` يبقى فرع متابعة تشغيلية للهدية، لكنه لا يستبدل سياسة ملكية الزبون المستفيد في فلتر "المسندة لي".

## قرار مكمّل — إنشاء وعد الهدية من العقد

إنشاء وعد الهدية في V1 يتم يدويا من نموذج العقد أو تفاصيل العقد الفعال.

الحقول التشغيلية المطلوبة في واجهة إنشاء الوعد:
- تعريف الهدية من `gift_definitions`.
- المستفيد: زبون العقد أو وسيط البيعة بحسب نوعه الفعلي (`customer_referrer`, `employee_referrer`, `personal_referrer`).
- شرط/سبب الوعد من قائمة شروط الهدايا.
- العدد الموعود `promised_quantity`.

القواعد:
- يجب قراءة معرف وسيط الزبون من الحقل العقدي الحقيقي `referrerId`، ولا يجوز التخمين من حقول غير موجودة مثل `referrerClientId` أو `clientId`.
- المستفيد الزبون يجب أن يرتبط بسجل `clients` معروف، والمستفيد الموظف بسجل موظف معروف، والمستفيد الشخصي بهوية ولقطة اسم قابلة للتتبع.
- إذا كان العقد `saleSubtype='free'`، يقترح النظام تعريف `gift_contract` وسبب `gift_contract`.
- الوعد الجديد يبدأ دائما بـ `condition_status='pending'`; لا تحسم حالة الشرط من نموذج الإنشاء.
- إنشاء الوعد لا ينشئ مهمة تسليم ولا يعتمد الكمية.
- إنشاء الوعد لا يحتاج أن يكون الشرط محققاً.
- في العقد المسودة، يبقى الوعد داخل `draft_gift_promises`.
- عند اعتماد العقد، تنشأ سجلات `gift_records.status='promised'` ومصادرها العقدية داخل معاملة الاعتماد نفسها.
- في العقد الفعال، ينشأ `gift_record` ومصدره العقدي مباشرة.
- قبل الحفظ، تعرض الواجهة الوعود غير المحققة المشابهة للمستفيد؛ التنبيه لا يمنع الحفظ لكنه يسجل قرار المتابعة.

## قرار مكمّل — مصدر الحقيقة في الواجهة

كل واجهة تعرض وعداً أو استحقاقاً أو تسليماً لهدية يجب أن تقرأ من `gift_records` عبر API، ولا يجوز لها إنشاء سجل عرض وهمي عند غياب البيانات.

القاعدة:
- `gift_records` هو مصدر الحقيقة الوحيد لظهور وعد مفتوح أو استحقاق أو تسليم.
- تبويب "الهدايا" داخل تفاصيل الزبون يجب أن يقرأ من `GET /gifts/records?clientId=:clientId`.
- صفحة "إدارة الهدايا" يجب أن تقرأ من `GET /gifts/records` مع فلاتر الحالة والنطاق.
- تفاصيل العقد يجب أن تعرض فقط السجلات المرتبطة فعلياً بالعقد أو المستفيد عبر `gift_records` و`gift_record_sources`.
- لائحة الأسماء المقترحة والاقتراح المباشر يمكن أن تعرض مسودة وعد داخل النموذج قبل الحفظ، لكن لا يجوز أن تظهر هذه المسودة في تبويب الزبون أو صفحة إدارة الهدايا قبل إنشاء `gift_record` فعلي.
- إذا لم توجد سجلات حقيقية لزبون ما، يجب أن يظهر جدول الهدايا فارغاً، ولا يجوز إنشاء سجل `promised` للمعاينة.

قواعد تعريف الهدية في الواجهة:
- كل منتقي "تعريف الهدية" يجب أن يقرأ من `gift_definitions` عبر API.
- لا يجوز استخدام قائمة prototype أو بيانات ثابتة كمصدر خيارات في مودلات إنشاء الوعد بعد ربط API.
- التعريفات غير الفعالة `is_active=false` لا تظهر في منتقي إنشاء وعد جديد، لكنها تبقى ظاهرة في السجلات التاريخية المرتبطة بها.

قواعد بيانات المعاينة:
- يسمح ببيانات mock/prototype فقط في مراحل التصميم البصري قبل ربط API.
- بعد ربط شاشة تشغيلية بالـ API، فشل التحميل يجب أن يظهر كحالة خطأ أو جدول فارغ، وليس كبيانات بديلة قابلة للالتباس.
- أي سجل معاينة يجب أن يكون موسوماً بوضوح كمعاينة، وألا يدخل في العدادات التشغيلية مثل "وعود مفتوحة" أو "جاهزة للتسليم".

اختبار قبول إلزامي:
- إذا كان الزبون لا يملك أي صف في `gift_records` حيث `beneficiary_client_id=:clientId` أو `customer_id=:clientId`، يجب أن يكون تبويب هدايا الزبون وصفحة إدارة الهدايا متطابقين: لا يظهر وعد مفتوح.
- إذا أُنشئ وعد من عقد أو لائحة أسماء أو اقتراح مباشر، يجب أن يظهر في صفحة إدارة الهدايا وفي تبويب الزبون فقط بعد نجاح إنشاء `gift_record`.
