# دستور فيتشر — توزيع المسارات ونطاق العمل

> الحالة: معتمد كمرجع دستوري للفيتشر
> اللغة: عربية موحّدة
> النطاق: Route Assignment
> الغرض: تثبيت القاعدة التشغيلية التي تربط الفريق بالمسارات والجغرافيا، وتحدّد كيف يُحتسب نطاق العمل الفعلي عند الحفظ، وما هي قواعد استبعاد الزبائن قبل إنتاج عبء العمل أو الأهداف اللاحقة.

---

## 0) الملخص التنفيذي

فيتشر توزيع المسارات هي الطبقة التي تربط الفريق اليومي بمساراته الجغرافية وبالمناطق الإضافية، ثم تحوّل هذا الربط إلى **نطاق عمل فعلي** قابل للاستخدام في التخطيط اللاحق.

هذه الفيتشر لا تكتفي بتسجيل المسارات، بل تحسم أيضًا من يدخل ضمن النطاق التشغيلي للفريق عند الحفظ. لذلك فهي تؤثر مباشرة على:
- حساب جهات الاتصال المؤهلة.
- توليد نطاق العمل.
- عبء الفريق.
- الأهداف اللاحقة المرتبطة بالتسويق والزيارات.

المسؤول المباشر عن هذا التوزيع هو **مدير الفرع** ضمن سياق فرعي صالح.

---

## 1) الفلو البشري المبسّط

`اختيار التاريخ → اختيار الفريق → تركيب المسارات → إضافة المناطق الإضافية → ترتيب المحطات → حفظ نطاق العمل → احتساب الجهات المؤهلة`

### شرح كل خطوة
- **اختيار التاريخ**: يحدد اليوم التشغيلي المطلوب.
- **اختيار الفريق**: يحدد الفريق المرتبط بجدول اليوم.
- **تركيب المسارات**: يختار المسارات المسموح إضافتها إلى نطاق الفريق.
- **إضافة المناطق الإضافية**: توسّع النطاق خارج المسارات الأصلية عند الحاجة.
- **ترتيب المحطات**: يحدد التسلسل التشغيلي للمناطق النهائية داخل النطاق.
- **حفظ نطاق العمل**: يثبت الربط في `route_assignments`.
- **احتساب الجهات المؤهلة**: يمرّر النظام على الزبائن المؤهلين داخل الفرع ويستبعد غير المطابقين قبل الاعتماد اللاحق.

---

## 2) حدود الفيتشر

### 2.1 داخل النطاق
تشمل هذه الفيتشر كل ما يلي:
- ربط الفريق بالمسارات المحددة مسبقًا.
- إضافة المناطق الإضافية يدويًا عبر البحث الجغرافي.
- ترتيب المحطات النهائية داخل النطاق.
- حفظ نطاق العمل في سجل مستقل.
- احتساب النطاق الفعلي للفريق من المسارات + المناطق الإضافية.
- تصفية جهات الاتصال المؤهلة ضمن الفرع قبل الاعتماد اللاحق.

### 2.2 خارج النطاق
لا تملك هذه الفيتشر:
- تعريف جدول الفرق نفسه.
- إنشاء الزيارة أو تعديل نتائجها النهائية.
- تعريف العملاء أو العقود ككيانات مستقلة.
- إدارة الصلاحيات العامة خارج سياق الفرع.

### 2.3 ممنوع الخلط بين الكيانات
- توزيع المسارات ليس جدول الفرق.
- المسار ليس الزيارة.
- المنطقة الإضافية ليست فريقًا.
- نطاق العمل ليس الأهداف النهائية.
- احتساب الجهات المؤهلة ليس عملية عرض فقط؛ بل شرط اعتماد تشغيلي.

---

## 3) خريطة المصدر

### 3.1 ملفات الخلفية الأساسية
- `packages/api/routes/routeAssignments.ts`
- `packages/api/routes/schedules.ts`
- `packages/api/routes/marketingVisits.ts`
- `packages/api/routes/workScopes.ts`
- `packages/api/routes/taskTypeConfig.ts` — مصدر إعدادات أنماط الجدولة ونوافذ `N`
- `packages/api/services/planningMarketingTargets.ts` — يحوي `buildOpenTaskEligibilityPredicate()`
- `packages/api/services/telemarketingScope.ts`

### 3.2 ملفات الواجهة الأساسية
- `packages/web/src/pages/planning/RouteAssigner.tsx`
- `packages/web/src/pages/planning/PlanOverview.tsx`
- `packages/web/src/pages/admin/TaskTypes.tsx` — الواجهة الإدارية لإعدادات أنماط الجدولة ونوافذ `N`
- `packages/web/src/components/GeoSmartSearch.tsx`

### 3.3 الملفات المشتركة المؤثرة
- `packages/shared/types.ts`
- `packages/web/src/lib/types.ts`
- `packages/web/src/lib/api.ts`

### 3.4 الملفات الدستورية المرتبطة
- `docs/constitution/domains/planning.md`
- `docs/constitution/features/team-scheduling.md`
- `docs/constitution/features/marketing-visits.md`

### 3.5 ملفات التصميم التحليلية
- `docs/analysis/task-scheduling-patterns.md` — التصميم الكامل لأنماط الجدولة الزمنية الأربعة
- `docs/analysis/task-model.md` — موديل المهمة الموحد + 11 حالة + 4 مراحل
- `migrations/106_task_type_config.sql` — جدول `task_type_config` + seed 20 نوع

---

## 4) المفاهيم الأساسية

### 4.1 `RouteAssignmentData`
بيانات ربط الفريق بالنطاق التشغيلي.
- تتكوّن من `routes` و`extraZones` و`stationOrder`.
- تُحفظ لكل يوم ولكل فريق بمفتاح مركّب.

### 4.2 `routes`
المسارات المختارة للفريق.
- كل مسار يقدّم جزءًا من الجغرافيا التشغيلية.
- يمكن إدخال جزء من المسار فقط عبر مؤشرات البداية والنهاية.

### 4.3 `extraZones`
المناطق الإضافية خارج المسارات.
- تُضاف يدويًا عند الحاجة.
- يجب أن تبقى غير مكررة وغير داخلة أصلًا في النطاق النهائي.

### 4.4 `stationOrder`
ترتيب المحطات النهائية داخل نطاق العمل.
- يستخدم لتثبيت التسلسل التشغيلي.
- يُنظَّف من أي عنصر خارج النطاق قبل الحفظ.

### 4.5 `work coverage`
النطاق التشغيلي النهائي بعد دمج المسارات والمناطق الإضافية.
- هو المخرج الحقيقي للفيتشر.
- عليه يُبنى حساب الجهات المؤهلة.

### 4.6 مهمة ضمن قيد الانتظار
المهمة المؤهلة للاحتساب هي مهمة تقع ضمن مرحلة **قيد الانتظار** وفق نموذج المهام الموحد.

حالات قيد الانتظار المعتمدة:
- `open`
- `needs_follow_up`

أما التأخر فهو بعد زمني أو عرض مشتق من `due_date`، وليس status ثالثاً داخل مرحلة قيد الانتظار.

كما يجب احترام البعد الزمني: المهمة اللاحقة خارج نافذة `N` لا تدخل حساب الحمل حتى لو كانت حالتها ضمن قيد الانتظار.

### 4.7 أنماط الجدولة الزمنية (`scheduling_pattern`)
قيمة `N` ليست رقماً موحّداً، بل تختلف **معنى** وحجماً حسب نمط المهمة. الأنماط الأربعة المعتمدة (تفاصيل كاملة في `docs/analysis/task-scheduling-patterns.md`):

| النمط | متى تدخل المهمة نطاق العمل؟ | أمثلة |
|------|------------------------------|------|
| `immediate` | فور إنشائها — `N` لا تنطبق | `emergency_maintenance`, `parts_sale`, `device_purchase` |
| `short_window` | إذا `due_date ≤ today + N` (نافذة قصيرة 3-7 أيام) | `device_delivery`, `device_installation`, `device_activation` |
| `long_window` | إذا `due_date ≤ today + N` (نافذة طويلة 15-30 يوم) | `periodic_maintenance`, `installment_collection` |
| `expected_window` | إذا `expected_date ≤ today + N` أو `expected_date IS NULL` | `device_demo` |

**قاعدة `NULL`:** إذا كان حقل النافذة (`due_date` أو `expected_date`) فارغاً، تبقى المهمة ضمن النطاق دائماً — لأن غياب التاريخ يعني "جاهزة الآن".

### 4.8 مصدر القيم (`task_type_config`)
كل القيم المرتبطة بنمط النوع ونافذته محفوظة في جدول `task_type_config`:
- `scheduling_pattern` — أحد الأنماط الأربعة
- `window_basis` — `none` / `due_date` / `expected_date`
- `planning_window_days` — قيمة `N` بالأيام (NULL للنمط `immediate`)
- `is_active` — تفعيل/تعطيل النوع بأكمله

التعديل يتم من واجهة الإدارة `/admin/task-types` (صلاحية `admin.task_types.manage`).

---

## 5) القاعدة التشغيلية الأساسية

### `RA-R001` — احتساب جهات الاتصال المؤهلة عند حفظ نطاق العمل
عند حفظ نطاق العمل للفريق، لا يجوز احتساب أي زبون إلا إذا كان يحقق الشروط التالية معًا:
1. أن يكون الزبون ضمن **الفرع الحالي**.
2. أن يكون الزبون **مسندًا إلى المشرفة أو الفني أو كليهما أو تابعًا للفرع** وفق الملكية التشغيلية المعتمدة.
3. أن تكون لديه **مهمة واحدة على الأقل ضمن مرحلة قيد الانتظار** (`open` أو `needs_follow_up`).
4. أن يكون نوع المهمة **مفعّلاً** في `task_type_config` (`is_active = TRUE`).
5. ألا تكون هذه المهمة **لاحقة خارج نافذة `N`** الخاصة بنوعها — وفق نمط الجدولة:
   - `immediate` → دائماً مؤهلة
   - `short_window` / `long_window` → `due_date IS NULL` أو `due_date ≤ today + N`
   - `expected_window` → `expected_date IS NULL` أو `expected_date ≤ today + N`

#### أثر القاعدة
- الزبون خارج الفرع يُستبعد.
- الزبون غير المسند تشغيليًا يُستبعد.
- الزبون الذي لا يملك مهمة ضمن قيد الانتظار يُستبعد.
- المهمة اللاحقة زمنياً تُستبعد من حساب الحمل حتى يحين دخولها نافذة العمل.
- المهمة من نوع معطّل في `task_type_config` تُستبعد تماماً.
- أي حالة خارج قيد الانتظار لا تكفي للاحتساب ضمن هذا المسار.

#### التطبيق التقني
الفلتر مُجسَّد في الدالة المساعدة `buildOpenTaskEligibilityPredicate(otAlias, ttcAlias)` ضمن `planningMarketingTargets.ts`. هذه الدالة تُستخدم في 4 مواضع:
1. الـ `LATERAL JOIN` لاختيار مهمة العميل في استعلام الأعداد.
2. الـ `EXISTS` للتحقق من وجود مهمة نشطة في استعلام الأعداد.
3. الـ `LATERAL JOIN` لاختيار مهمة العميل في استعلام التفاصيل.
4. الـ `EXISTS` للتحقق من وجود مهمة نشطة في استعلام التفاصيل.

أي تعديل على قاعدة الاحتساب يجب أن يتم في الدالة المساعدة فقط — لضمان التطابق بين كل المواضع.

#### لماذا هذه القاعدة مهمة
هذه القاعدة هي البوابة الفعلية التي تمنع تضخيم نطاق العمل أو إدخال جهات اتصال غير مؤهلة، وتمنع تحميل الفريق بزخم تشغيلي غير صحيح.

---

## 6) قواعد التشغيل والبيانات

### `RA-R002` — حفظ النطاق لا يتم بلا فريق صالح
- يجب اختيار الفريق قبل حفظ أي نطاق عمل.
- السجل النهائي مرتبط بتاريخ + فريق.

### `RA-R003` — لا تكرار للمسارات
- لا يجوز إدخال المسار نفسه أكثر من مرة في نفس النطاق.

### `RA-R004` — لا تكرار للمناطق الإضافية
- لا يجوز تكرار المنطقة الإضافية داخل `extraZones`.

### `RA-R005` — ترتيب المحطات يجب أن يبقى داخل النطاق
- أي محطة خارجة عن النطاق النهائي تُزال قبل الحفظ.

### `RA-R006` — مدير الفرع هو المسؤول المباشر
- التوزيع التشغيلي لنطاق العمل مسؤولية مدير الفرع ضمن سياق الفرع الفعّال.

---

## 7) الصلاحيات والنطاق

### 7.1 الصلاحية الأساسية
- `marketing_visits.view`
- `marketing_visits.update_result`

### 7.2 صلاحيات إعدادات أنماط الجدولة
- `admin.task_types.view` — عرض إعدادات أنواع المهام (الصفحة الإدارية)
- `admin.task_types.manage` — تعديل قيم `N` وتفعيل/تعطيل الأنواع
- مفتوح لكل المستخدمين المصادَق عليهم في الـ GET (لأن محرك التخطيط يحتاج القراءة)
- محصور بصلاحية `manage` في الـ PATCH

### 7.3 النطاق
- فيتشر التوزيع **branch-only**.
- لا يُسمح بالحفظ أو القراءة التشغيليّة خارج سياق فرع فعّال.
- هذا المسار لا يعمل كمسار HQ عام.
- صفحة إدارة الأنواع (`/admin/task-types`) **شركية** — تطبق على كل الفروع.

---

## 8) الفجوات المعروفة

### ~~`RA-G001`~~ — ✅ مُغلقة 2026-05-17
- ~~مصدر قيمة `N` غير مثبت بعد~~ ← أصبح `task_type_config` (migration 106)
- النمط الزمني يُحدَّد عبر `scheduling_pattern` و`window_basis` و`planning_window_days`.
- التعديل من واجهة `/admin/task-types` بصلاحية `admin.task_types.manage`.
- التفاصيل الكاملة في `docs/analysis/task-scheduling-patterns.md`.

### `RA-G002` — يجب عدم استخدام `pending` كاسم حالة Canonical
- التعبير التشغيلي المعتمد هو **مرحلة قيد الانتظار**، لا status باسم `pending`.
- حالات المرحلة المعتمدة هي: `open`, `needs_follow_up`.
- التأخر قراءة زمنية مشتقة، وليس status باسم `overdue` ضمن نموذج الـ 11 حالة.
- أي كود أو توثيق يستخدم `pending` يجب أن يُعامل كلغة legacy أو وصف عام، لا كقيمة status معتمدة.

### `RA-G003` — التعميم على بقية أنواع المهام
- حالياً الفلتر مقيد بـ `task_type = 'device_demo'` في كلا الاستعلامين (counts + leads).
- البنية جاهزة لاستقبال أي نوع آخر عبر `task_type_config`، لكن القيد الصريح في الكود يجب رفعه يدوياً عند تعميم الفيتشر.
- عند رفع القيد، تصبح كل المهام من كل الأنواع المفعّلة قابلة للاحتساب وفق نمطها الزمني المعتمد.

---

## 9) الواجهة المرتبطة

### 9.1 الصفحة الأساسية
- `packages/web/src/pages/planning/RouteAssigner.tsx`

### 9.2 ما تعرضه الواجهة
- التاريخ.
- اختيار الفريق.
- تركيب المسارات.
- المناطق الإضافية.
- ترتيب المحطات.
- حفظ نطاق العمل.
- ملاحظات الحساب أو الحفظ.

### 9.3 مصدر القيم
- الفرق اليومية تأتي من `api.schedules.get(date)`.
- المسارات تأتي من `api.routes.list()`.
- الجغرافيا تأتي من `api.geoUnits.list()`.
- الحفظ يتم عبر `api.routeAssignments.save(key, data)`.

---

## 10) واجهات الـ API

### 10.1 مسارات ربط الفريق
- `GET /route-assignments`
- `GET /route-assignments/:key`
- `PUT /route-assignments/:key`

### 10.2 مسارات ذات علاقة مباشرة
- `GET /schedules/:date`
- `GET /planning/marketing-targets?date=...&teamKey=...` *(اسم تقني تاريخي؛ المعنى التشغيلي الحالي هو جهات اتصال ذات مهام ضمن النطاق)*
- `GET /work-scopes/:date/:teamKey`
- `POST /work-scopes`

### 10.3 مسارات إدارة أنماط الجدولة
- `GET /admin/task-types` — قائمة الأنواع وأنماطها وقيم `N` (مفتوح لكل مصادَق عليه)
- `GET /admin/task-types?activeOnly=true` — الأنواع المفعّلة فقط
- `PATCH /admin/task-types/:taskType` — تعديل `planningWindowDays` و`isActive` (صلاحية `admin.task_types.manage`)

---

## 11) التوافق الخلفي

- `route_assignments` هو المصدر الفعلي لحفظ ربط الفريق بالنطاق.
- `RouteAssigner` يركب النطاق من المسارات والمناطق الإضافية.
- `planning/marketing-targets` يستهلك هذا الربط لاحقًا في احتساب جهات الاتصال ذات المهام والجهات المؤهلة.
- أي تعديل على معيار الاحتساب يجب أن يمر عبر هذه الفيتشر أولًا قبل أن ينعكس على الأهداف أو الزيارات.

---

## 12) الخلاصة

فيتشر توزيع المسارات هي عقد تشغيلي لا يقتصر على حفظ المسارات، بل يحدد من يدخل ضمن النطاق الفعلي للفريق. القاعدة الجوهرية فيها هي أن الزبون لا يُحتسب إلا إذا كان داخل الفرع، ومسندًا تشغيليًا بالشكل الصحيح، ويمتلك مهمة ضمن **قيد الانتظار** وليست لاحقة خارج نافذة `N` المعرّفة في `task_type_config` حسب نمط جدولة النوع.

---

## 13) سجل التغييرات الجوهرية

| التاريخ | التغيير | المرجع |
|--------|---------|--------|
| 2026-05-17 | إنشاء `task_type_config` + seed 20 نوع + 4 أنماط جدولة | migration 106, `task-scheduling-patterns.md` |
| 2026-05-17 | استبدال الفلتر القديم (8 حالات) بـ helper موحّد `buildOpenTaskEligibilityPredicate` | `planningMarketingTargets.ts` |
| 2026-05-17 | إغلاق `RA-G001` (مصدر `N` أصبح مثبتاً) | هذا الملف §8 |
| 2026-05-17 | إصلاح bug `column e.is_active does not exist` في `resolveMarketingActorHrUserIds` | `planningMarketingTargets.ts` |
| 2026-05-17 | إضافة الواجهة الإدارية `/admin/task-types` + صلاحيتي `view`/`manage` | `TaskTypes.tsx`, migration 106 |
| 2026-05-17 | فتح فجوة `RA-G003` (التعميم على بقية الأنواع) | هذا الملف §8 |
