# خطة تنفيذ واجهة نظام الهدايا — مراحل قابلة للاختبار

> **الحالة:** محدثة وفق تسوية الدستور 2026-07-26 — التنفيذ قيد المطابقة
> **الهدف:** تسليم النظام تدريجيا للمراجعة والاختبار، دون انتظار اكتمال كل المسارات الخلفية دفعة واحدة.

---

## مبادئ التسليم

- كل مرحلة يجب أن تعرض تجربة UI قابلة للاختبار يدويا.
- لا تعتمد الواجهة على إخفاء الأزرار كحماية؛ الخادم لاحقا يجب أن يفرض نفس القواعد.
- لا إدارة مخزون في V1.
- لا تسليم جزئي.
- لا إنشاء تلقائي للوعود.
- لا دمج تلقائي للوعود أو جمع للكمية من المصادر.
- إنشاء مهمة `gift_delivery` خطوة مستقلة بعد الاعتماد.
- يمكن لمهمة واحدة جمع عدة سجلات لنفس المستفيد والفرع بنتيجة جماعية غير جزئية.
- طريقة التنفيذ، لا نوع المستفيد، هي التي تحدد استخدام مهمة ميدانية أو تسليم يدوي.
- بعد ربط أي شاشة بالـ API، لا يجوز استخدام mock/prototype fallback يعرض سجلات هدايا تشغيلية. فشل التحميل يظهر كخطأ أو كجدول فارغ.
- كل منتقي تعريف هدية في مودلات إنشاء الوعد يجب أن يقرأ من `gift_definitions` عبر API، وليس من قائمة ثابتة.

---

## المرحلة 0 — الأساس المرئي والـ read-only mocks

### الهدف

إثبات المفهوم بصريا قبل schema الكامل.

### واجهات

- صفحة مركزية باسم "إدارة الهدايا".
- تبويب "الهدايا" داخل تفاصيل الزبون.
- قسم "هدايا العقد" داخل تفاصيل العقد.

### ما يختبره المستخدم

- الفلاتر الأساسية: الحالة، حالة الشرط، الفرع المسؤول، المسند لي، تعريف الهدية.
- قراءة سجل هدية mock كامل.
- رؤية مصادر السجل تحت السجل.
- رؤية الفرق بين `status` و`condition_status`.

### قبول المرحلة

- لا توجد عمليات كتابة.
- التصميم واضح للمراجعة المنتجية.
- المسميات العربية متطابقة مع الدستور.
- أي بيانات mock في هذه المرحلة يجب أن تكون موسومة كمعاينة ولا تدخل في عدادات تشغيلية بعد الانتقال إلى مراحل الربط.

---

## المرحلة 1 — تعريفات الهدايا

### الهدف

إدارة `gift_definitions`.

### واجهات

- جدول تعريفات الهدايا.
- نموذج إنشاء/تعديل:
  - الاسم.
  - الوصف.
  - `default_unit_label`.
  - فعال/غير فعال.

### ما يختبره المستخدم

- إنشاء تعريف هدية.
- تعديل تعريف.
- تعطيل تعريف.
- استخدام `default_unit_label` في العرض.

### قبول المرحلة

- لا توجد أي حقول مخزون أو رصيد.
- التعريفات غير الفعالة لا تظهر في منتقي إنشاء سجل جديد.

---

## المرحلة 2 — إنشاء سجل هدية يدوي

### الهدف

إنشاء `gift_records` و`gift_record_sources` يدويا.

### واجهات

- من تفاصيل العقد: زر "إضافة وعد هدية".
- من الصفحة المركزية: زر "إضافة سجل هدية".
- نموذج السجل:
  - المستفيد.
  - تعريف الهدية.
  - شرط الوعد من `gift_promise_conditions`.
  - المصدر.
  - الكمية.
  - الفرع المسؤول.
  - المسؤول الإداري.
  - ملاحظات.

### ما يختبره المستخدم

- إنشاء وعد لصاحب العقد.
- إنشاء وعد لوسيط بيعة من نوع زبون.
- إنشاء وعد لوسيط موظف أو شخصي بهوية قابلة للتتبع.
- محاولة إنشاء وعد لوسيط `Unknown` يجب أن تمنع في العقود الجديدة أو تظهر كبيانات غير صالحة.
- عرض تحذير غير مانع عند وجود وعد غير محقق مشابه من عقد أو لائحة أسماء أو اقتراح مباشر.
- المتابعة بعد التحذير تنشئ سجلا مستقلا وتسجل قرار المستخدم.
- ربط مصدر إثبات يدويا بوعد موجود لا يغير الكمية أو حالة الشرط.
- وعود العقد المسودة لا تظهر كسجلات تشغيلية قبل اعتماد العقد.

### قبول المرحلة

- السجل يبدأ بـ `status='promised'`.
- السجل يبدأ بـ `condition_status='pending'`.
- اعتماد العقد يحول كل `draft_gift_promises` داخل معاملة واحدة؛ فشل أي تحويل يمنع الاعتماد ويحفظ المسودات.
- لا يحدث اعتماد ولا مهمة تسليم في هذه المرحلة.

---

## المرحلة 3 — المتابعة الإدارية وحالة الشرط

### الهدف

تمكين مسؤول الهدايا من إدارة `condition_status` والإسناد.

### واجهات

- لوحة إجراءات على سجل الهدية:
  - "الشرط تحقق".
  - "الشرط لم يتحقق".
  - "إعادة إلى قيد المراجعة".
  - تغيير المسؤول الإداري.
  - تغيير الفرع المسؤول.

### ما يختبره المستخدم

- تحديث `condition_status`.
- ظهور من حدث الحالة ومتى.
- فلتر "المسند لي".
- فلتر كل الفروع/فرع واحد حسب الصلاحية.

### قبول المرحلة

- تغير حالة الشرط لا يغير `status`.
- يمكن بقاء سجل `promised` مع `condition_status='met'` أو `not_met`.

---

## المرحلة 4 — الاعتماد للتسليم

### الهدف

تجميد الكمية وتحويل السجل إلى `approved_for_delivery`.

### واجهات

- زر "اعتماد للتسليم".
- شاشة تأكيد تعرض:
  - تعريف الهدية.
  - `promised_quantity`.
  - المستفيد.
  - الشرط وحالته.
  - ملاحظات اعتماد.

### ما يختبره المستخدم

- اعتماد سجل شرطه `met`.
- اعتماد سجل شرطه `pending`.
- اعتماد سجل شرطه `not_met` مع إلزام الملاحظات.
- محاولة إعادة الاعتماد لتعديل الكمية يجب أن تمنع.
- سحب الاعتماد قبل إنشاء مهمة يتطلب سببا ويعيد السجل إلى `promised`.

### قبول المرحلة

- `approved_quantity` تصبح لقطة ثابتة.
- `status='approved_for_delivery'`.
- تحفظ هوية المعتمد والوقت والملاحظات.
- لا تنشأ مهمة تلقائيا.

---

## المرحلة 5 — إنشاء مهمة تسليم الهدية

### الهدف

إنشاء `gift_delivery` من سجل معتمد.

### واجهات

- اختيار سجل واحد أو عدة سجلات ثم زر "إنشاء مهمة تسليم".
- نموذج:
  - تاريخ التسليم المطلوب.
  - الأولوية.
  - ملاحظات.

### قواعد UI

- يظهر الزر فقط إذا:
  - كل السجلات `status='approved_for_delivery'`.
  - كل السجلات لنفس المستفيد الزبون ونفس الفرع المسؤول.
  - لا توجد مهمة نشطة لأي سجل.
- يعرض تحذير واضح بأن نتيجة المهمة جماعية على كل السجلات المختارة.
- يظهر إجراء "تأكيد تسليم يدوي" أيضا للزبون إذا كان التنفيذ دون زيارة.

### ما يختبره المستخدم

- إنشاء مهمة لصاحب العقد.
- إنشاء مهمة لوسيط بيعة من نوع زبون، والمهمة تكون على زبون الوسيط.
- إنشاء مهمة تجمع عدة سجلات لنفس الزبون والفرع.
- منع تجميع مستفيدين أو فرعين مختلفين.
- منع إنشاء مهمة لوسيط موظف أو شخصي.

### قبول المرحلة

- كل السجلات المختارة تنتقل إلى `delivery_task_created`.
- تظهر المهمة في جدول "مهام تسليم الهدايا".
- المهمة تعرض كل الهدايا والكميات ووحدات العرض والمصادر المرتبطة.

---

## المرحلة 6 — تسجيل نتيجة التسليم

### الهدف

إغلاق مسار `gift_delivery`.

### واجهات

- Modal نتيجة `gift_delivery`:
  - تم التسليم بنجاح.
  - رفض الهدية.
  - إعادة جدولة.
- لا يظهر أي خيار رابع أو كمية مسلمة أو نتيجة مستقلة لكل سجل.
- عند النجاح: checkbox "أقر بأن كامل الكمية المعتمدة تم تسليمها".

### ما يختبره المستخدم

- النجاح يتطلب الإقرار.
- لا يوجد حقل كمية مسلمة.
- إعادة الجدولة تتطلب سبباً من قائمة `gift_delivery_reschedule_reasons` وتاريخ متابعة فقط.
- رفض الهدية يغلق السجل كـ `refused`.
- النجاح يغلق السجل كـ `delivered`.
- عند التجميع، تطبق النتيجة نفسها على كل السجلات ولا توجد نتيجة منفردة لكل سجل.
- إلغاء المهمة دون زيارة فعالة يعيد السجلات إلى `approved_for_delivery` ولا يسجل رفضا.
- بعد إقفال الزيارة، يمر التصحيح عبر إعادة فتحها ثم تعديل النتيجة من داخلها.

### قبول المرحلة

- لا تسليم جزئي.
- نافذة النتيجة تفتح من الزيارة فقط؛ تفاصيل المهمة تعرض النتيجة للقراءة دون زر تسجيل.
- reflection واضح على `gift_records`.
- reflection عكسي صحيح عند تغيير نتيجة نهائية بعد إعادة فتح الزيارة.
- تبويب الزبون يعرض الهدية ضمن "تم استلامه".

---

## المرحلة 7 — التأكيد اليدوي

### الهدف

دعم كل حالات التنفيذ دون زيارة.

### واجهات

- زر "تأكيد تسليم يدوي".
- يظهر فقط إذا:
  - السجل معتمد.
  - لا توجد مهمة تسليم نشطة.
  - لم يتم التسليم بعد.
- اختيار طريقة التنفيذ: استلام من الشركة، تسليم مباشر، تسليم عقد، أو غير ذلك.
- إقرار بتنفيذ كامل الكمية المعتمدة.

### ما يختبره المستخدم

- تأكيد تسليم هدية لموظف.
- تأكيد تسليم هدية لوسيط شخصي.
- تأكيد استلام زبون من الشركة.
- تأكيد تنفيذ هدية من نوع عقد دون زيارة.
- حفظ ملاحظات التسليم اليدوي.

### قبول المرحلة

- `status='delivered_manually'`.
- لا تنشأ مهمة.
- تحفظ الطريقة والمنفذ والوقت والفرع والملاحظات.
- إعادة الفتح تتطلب صلاحية إدارية وسببا، وتعيد السجل إلى `approved_for_delivery`.
- يظهر السجل في تبويب الزبون/الصفحة المركزية كمسلم يدويا.

---

## المرحلة 8 — الصلاحيات والتدقيق

### الهدف

إغلاق النظام أمنيا قبل staging.

### المطلوب

- migrations للصلاحيات.
- إضافة `contract_gifts.reopen_manual_delivery` إلى الكتالوج والمنح المقيدة وملف `صلاحيات_النظام.xlsx`.
- policy لـ `gift_records`.
- list scope على الصفحة المركزية.
- تحديث `صلاحيات_النظام.xlsx`.
- اختبارات:
  - غياب الصلاحية.
  - فرع خاطئ.
  - سجل غير مسند عند نطاق `ASSIGNED`.
  - تجاوز تحذير وعد مشابه مع تسجيل التدقيق.
  - محاولة تجميع سجلات لمستفيدين أو فرعين مختلفين.
  - إلغاء مهمة مجمعة قبل الزيارة وإعادة كل السجلات.
  - تصحيح نتيجة مجمعة بعد إعادة فتح زيارة.
  - إعادة فتح تسليم يدوي بسبب موثق.
  - super-admin path.
  - `GLOBAL`.
  - `BRANCH`.
  - `ASSIGNED`.

### قبول المرحلة

- لا endpoint يعتمد على UI gate.
- audit permissions لا يظهر مفاتيح غير معرفة.
- الصلاحيات تظهر في واجهة الأدوار.

---

## ترتيب التسليم المقترح للمراجعة

1. المرحلة 0: مراجعة تجربة القراءة.
2. المرحلة 1: تعريفات الهدايا.
3. المرحلة 2: إنشاء سجل هدية ومصادره.
4. المرحلة 3 و4 معا: تحقق الشرط والاعتماد.
5. المرحلة 5: إنشاء مهمة التسليم.
6. المرحلة 6: نتيجة التسليم.
7. المرحلة 7: التأكيد اليدوي.
8. المرحلة 8: إغلاق الصلاحيات والاختبارات.

---

## اختبارات قبول مصدر الحقيقة في الواجهة

هذه الاختبارات إلزامية عند كل مرحلة تنتقل من mock إلى API:

- تبويب "الهدايا" في تفاصيل الزبون يقرأ من `GET /gifts/records?clientId=:clientId`.
- صفحة "إدارة الهدايا" تقرأ من `GET /gifts/records`.
- إذا لم توجد سجلات حقيقية للزبون في `gift_records`، يجب أن يكون تبويب الزبون فارغاً ولا تظهر عدادات وعود مفتوحة.
- لا يجوز لتبويب الزبون أو صفحة إدارة الهدايا استدعاء helper يولد سجل معاينة مثل `getClientGiftRecords` بعد ربط API.
- وعد الهدايا من لائحة الأسماء أو الاقتراح المباشر يبقى مسودة داخل المودل حتى يحفظ كـ `gift_record`.
- وعد العقد يبقى في `draft_gift_promises` حتى اعتماد العقد، ولا يظهر كسجل تشغيلي قبل ذلك.
- بعد حفظ الوعد بنجاح، يجب أن يظهر نفس السجل في الصفحة المركزية وفي تبويب المستفيد، بنفس `id` و`status`.
- إذا فشل تحميل `gift_definitions`، يجب تعطيل منتقي تعريف الهدية أو عرض رسالة خطأ، ولا يجوز ملؤه من قائمة prototype.

---

## ملاحظات خارج نطاق V1

- إدارة مخزون الهدايا.
- تسليم جزئي.
- محرك شروط آلي.
- إنشاء تلقائي من العقود أو الدفعات.
- توقيع/صورة/رقم تسلسلي/مرفقات خاصة بتعريف الهدية.
