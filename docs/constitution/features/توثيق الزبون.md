# دستور الفيتشر — توثيق الزبون

> الحالة: مسودة
> 
> هذا الملف يوثّق المسارات التشغيلية الخاصة بزيارة الزبون، المهام الميدانية، الصيانة، التحصيل، النقل، والكفالات كما وردت في ملف «مهام الزيارات».

## 1. الملخص التنفيذي

هذا الفيتشر يجمع كل المهام التي ينفذها الفريق مع الزبون بعد البيع أو أثناء المتابعة الميدانية. الهدف منه هو توحيد دورة العمل من لحظة عرض الجهاز وحتى التركيب والتشغيل والصيانة والتحصيل والنقل والكفالات، بحيث تكون لكل مهمة نتيجة واضحة وحالة تشغيلية مفهومة.

الملف الموثّق يغطي 20 مهمة تشغيلية رئيسية.

## 2. العقد التشغيلي

### 2.1 حدود الفيتشر

يشمل هذا الفيتشر:
- مهمة عرض الجهاز
- مهام البيع والتسليم والتركيب والتشغيل
- مهام الصيانة الدورية والطارئة
- مهام التحصيل المالي
- مهام الهدايا والتشييك
- مهام القطع المباعة دون تركيب
- مهام سحب الجهاز وإصلاحه وإعادته
- مهام الكفالة والتوقيف والنقل

ولا يشمل:
- قواعد المحاسبة التفصيلية خارج ما يدوّنه الفريق في المهمة
- تصميم الواجهة أو شكل الإيصالات بشكل نهائي
- منطق التسعير الداخلي غير المذكور في الملف المصدر

### 2.2 المسار التشغيلي العام

المسار العام للزبون يمر عادة عبر المراحل التالية:
1. عرض الجهاز وتقديم الشرح والفحص الأولي
2. الانتقال إلى البيع أو المتابعة أو الرفض
3. عند وجود موافقة، يتم إنشاء عقد شراء أو متابعة تقسيط
4. يتم تسليم الجهاز ثم تركيبه ثم تشغيله
5. بعد التشغيل تبدأ الصيانة الدورية والمتابعة اللاحقة
6. عند الأعطال أو الطلبات الخاصة تنتقل العملية إلى الصيانة الطارئة أو سحب الجهاز أو إعادة تركيبه
7. عند الحاجة يمكن تنفيذ التحصيل، الهدايا، الكفالات، الإلغاء، أو النقل

### 2.3 قائمة المهام الموثّقة

#### 1) مهمة عرض الجهاز
- تتم عند وصول الفريق إلى منزل الزبون.
- تشمل استبيان، فحص المياه، وتجربة الديمو كيت.
- تنتهي بتقديم عرض سعري وتحديد نوع العرض.
- نتائجها المحتملة: شراء، متابعة لاحقة، عدم قدرة مادية، أو عدم اهتمام.

#### 2) مهمة شراء جهاز
- تنشأ بعد قبول الشراء.
- يتحدد فيها نوع الجهاز، السعر، العملة، الدفعة الأولى، والأقساط.
- يتم توثيق تواريخ الأقساط بشكل يدوي.

#### 3) مهمة تسليم الجهاز
- يتم فيها تسليم الجهاز بعد كتابة عقد الشراء.
- التسليم يكون عبر إيصال يحدد نوع الجهاز.
- لا يُشترط فيها إثبات حالة فنية كاملة لأن الجهاز لم يُشغّل بعد.

#### 4) مهمة تركيب الجهاز
- يقوم الفني بتركيب الجهاز وإضافة الملحقات.
- يتم تسجيل القطع المستخدمة وما أُعيد من الملحقات الأساسية.
- لا يلزم ذكر الحالة الفنية النهائية لأن الجهاز لم يبدأ بالعمل بعد.

#### 5) مهمة تشغيل الجهاز
- تتم بعد التركيب ووصل المياه.
- يبدأ معها التشغيل الفعلي.
- من هذه المهمة يبدأ احتساب موعد الصيانة الدورية الأولى.
- يجب توثيق الحالة الفنية الكاملة عند التشغيل.

#### 6) مهمة الصيانة الدورية للجهاز
- تتضمن استبياناً، حالة فنية قبل الصيانة، وإبلاغ الزبون بالقطع الواجب تبديلها.
- بعد الموافقة يتم تبديل القطع المتفق عليها ثم توثيق الحالة بعد التبديل.
- تعتبر منفذة إذا تم تبديل قطعة واحدة على الأقل من قطع الدورية.
- تعتبر غير منفذة إذا لم يتم تبديل أي قطعة أو إذا طلب الزبون التأجيل.

#### 7) مهمة الصيانة الطارئة للجهاز
- تخص الأعطال غير المرتبطة بقطع الدورية.
- تشمل الاستبيان، الحالة الفنية قبل الصيانة، وتحديد القطع المطلوبة.
- تعتبر منفذة إذا تم تبديل القطعة أو تم حل المشكلة دون تبديل.
- تعتبر غير منفذة إذا تعذر الحل بسبب الفني أو تم الاتفاق على استبدال الجهاز أو سحبه للشركة.

#### 8) مهمة تحصيل قسط جهاز
- تتم عند استحقاق القسط المحدد.
- يمكن أن تنتهي بعدة حالات: دفع القسط، تسكير كامل المبلغ، دفع جزئي، أو تأجيل مع تاريخ جديد.
- يجب أن يظهر أثر التأخير إذا حصل تأجيل.
- الملف يطرح أيضاً حالات دفع جزء من القسط الحالي أو جزء من قسط لاحق كنقطة تحتاج ضبطاً تشغيلياً أدق.

#### 9) مهمة تحصيل ذمة الصيانة
- تعمل بنفس مبدأ تحصيل القسط.
- الهدف منها استيفاء المبالغ المستحقة على الزبون لقاء أعمال الصيانة.

#### 10) مهمة تسليم هدية
- يتم فيها تسليم هدية متفق عليها مسبقاً أو هدية وسيط.
- يجب توثيق السبب وصاحب القرار ونوع الهدية.
- تعتبر منفذة عند التسليم، وغير منفذة عند عدمه.

#### 11) مهمة تشييك على الجهاز
- زيارة مخصصة غالباً بعد تركيب جهاز جديد.
- هدفها التأكد من العيار والحالة الفنية.
- تتضمن أيضاً إمكانية أخذ أسماء مقترحة من الزبون لأغراض تسويق لاحقة.

#### 12) مهمة شراء قطعة دون تبديل
- يقوم الفريق ببيع قطعة أو أكثر للزبون دون تركيبها مباشرة.
- يجب تسجيل القطع والسعر والتسكير.
- يجب أن يحتفظ النظام بإشارة إلى القطع غير المستخدمة للتذكير بها لاحقاً.

#### 13) مهمة سحب الجهاز للشركة للصيانة
- تتم بطلب فني أو بطلب الزبون.
- يجب تسجيل نوع الجهاز وما تم سحبه معه.
- يجب توضيح هل تم سحب الجهاز وحده أم مع خزان وحنفية أو ملحقات أخرى.

#### 14) مهمة فحص الجهاز وإصلاحه بالشركة
- بعد وصول الجهاز للشركة يتم فحصه وتقديم حالة فنية قبل الإصلاح.
- يتم تحديد القطع المطلوبة وتكلفة الإصلاح والتواصل مع الزبون.
- عند الموافقة يتم الإصلاح وتوثيق المبلغ كذمة صيانة.
- عند الرفض تعتبر المهمة غير منفذة مع ذكر السبب.

#### 15) مهمة إعادة الجهاز بعد الصيانة
- تتم بعد الانتهاء من إصلاح الجهاز في الشركة.
- يتضمن المسار إعادة الجهاز للزبون وتركيبه وتشغيله من جديد.
- يجب تسجيل الحالة الفنية في منزل الزبون بعد الإرجاع.

#### 16) مهمة منح كفالة ذهبية للجهاز
- تتم بعد تقديم حالة فنية وبناء على قرار من مدير مختص.
- يجب تحديد مدة الكفالة وسعرها والقطع المشمولة وشروطها.

#### 17) مهمة إلغاء كفالة الجهاز الأساسية
- يحصل الإلغاء عندما يفقد الزبون الالتزام بشروط الصيانة أو الصيانة الطارئة.
- بعد الإلغاء يمكن احتساب أجور ومواصلات إضافية على أي خدمة لاحقة.
- يبقى الإلغاء سارياً حتى إعادة الالتزام.

#### 18) مهمة إعادة تفعيل كفالة الجهاز الأساسية
- تتم بعد تواصل الزبون وإعادة الالتزام بشروط الصيانة.
- تتطلب تبديل القطع اللازمة وتوثيق الحالة قبل وبعد.
- يجب تسجيل القطع والتكاليف والمبلغ المدفوع والتسكير.

#### 19) مهمة توقيف الجهاز عن العمل بشكل مؤقت
- تتم بطلب الزبون لفصل الجهاز مؤقتاً عن المياه.
- الأسباب المذكورة: سفر، إعادة إكساء، أو نقل لاحق.
- الهدف منها حفظ الجهاز مؤقتاً دون إنهاء العلاقة التشغيلية معه.

#### 20) مهمة نقل الجهاز إلى عنوان جديد
- تتم عند نقل الجهاز ضمن نفس الفرع أو بين فروع مختلفة.
- قد يشمل النقل الفك من فرع وتركيبه وتشغيله في فرع آخر.
- يجب اعتبار النقل مساراً مستقلاً يربط العنوان القديم بالعنوان الجديد.

### 2.4 قواعد عامة

- كل مهمة يجب أن يكون لها سبب واضح ونتيجة واضحة.
- المهام المنفذة يجب أن تنتج إيصالاً أو حالة توثيقية مناسبة بحسب نوع المهمة.
- إذا احتاجت المهمة إلى موافقة الزبون، فيجب توثيق الموافقة قبل إتمامها.
- إذا كانت المهمة مرتبطة بقطع، فيجب ذكر القطع المستخدمة أو المبدلة أو المسحوبة.
- إذا كانت المهمة مرتبطة بتسعير أو تحصيل، فيجب توثيق المبلغ المدفوع والإجمالي والحسم إن وجد.
- إذا كانت المهمة مرتبطة بالتأجيل، فيجب تسجيل التأخير والموعد الجديد.

### 2.5 النتائج العامة للمهمة

هذا الملف يذكر النتائج التشغيلية من منظور العمل فقط، ولا يعرّف حالات `open_tasks.status`. الحالات العامة والمرحلة والبعد الزمني تُحكم حصراً من:

- `docs/constitution/domains/tasks.md`

النتائج المتكررة عبر معظم المهام هي:
- منفذة
- غير منفذة كهدف نوعي، مع سبب واضح
- تحتاج متابعة
- ملغاة
- مكتملة جزئياً حيث ينطبق ذلك على التحصيل أو التبديل

هذه النتائج تتحول إلى الحالة العامة المناسبة مثل `completed` أو `needs_follow_up` أو `cancelled` أو `closed` حسب عقد نوع المهمة.

### 2.6 ملاحظات تشغيلية

- الصيانة الدورية تبدأ فقط بعد تشغيل الجهاز.
- بعض المهام تنتج أثراً مستقبلياً، مثل القطع المشتراة دون استخدام أو الذمة المالية أو الكفالة.
- بعض السيناريوهات تحتاج قواعد أدق لاحقاً، خصوصاً التحصيل الجزئي، والتأجيل المتكرر، والربط بين المهام المتسلسلة.

### 2.7 عقد الوسيط في الزبون

هذا القسم يوثّق عقد الوسيط المستخدم في نموذج `إضافة زبون جديد` ونموذج تعديل الزبون داخل `packages/web/src/components/ClientModal.tsx`.

#### القواعد التشغيلية
- الوسيط ليس كياناً مستقلاً؛ هو مجموعة حقول داخل سجل الزبون.
- النوع الموثق في الواجهة هو: `Personal`، `Employee`، `Client`، `Unknown`.
- `sourceChannel` قيمة إلزامية تشغيلياً، والواجهة تعرض فقط: `Acquaintance`، `PhoneCall`، `SocialMedia`، `Campaign`.
- قيمة `App` قيمة تراثية مقبولة في الكود، وتُحوَّل إلى `SocialMedia` قبل الحفظ أو العرض؛ لا تظهر كخيار مباشر في الواجهة الحالية.
- `Personal` يملأ اسم الوسيط تلقائياً باسم المستخدم الحالي ويصفّر `referralEntityId`.
- `Unknown` يملأ اسم الوسيط تلقائياً بالقيمة `مجهول` ويصفّر `referralEntityId`.
- `Employee` يتطلب اعتماد رقم وظيفي صالح حتى يُثبت اسم الوسيط من سجل الموظف.
- `Client` يتطلب اختيار زبون موجود من البحث الداخلي حتى يُثبت الاسم و`referralEntityId`.
- `referralNotes` ملاحظات نصية قابلة للتحرير دائماً.
- في وضع `fromCandidate` تُقفل حقول الوسيط المنقولة من الاسم المقترح ولا تُعدّل يدوياً.

#### الحقول المرسلة عند الحفظ
- `referrerType`
- `sourceChannel`
- `referrerName`
- `referralEntityId`
- `referralNotes`

#### قواعد الخلفية
- `packages/api/routes/clients.ts` يفرض على `Personal` أن يكون `referrerName` هو اسم المستخدم الحالي.
- `packages/api/routes/clients.ts` يصفّر `referrerId` و`referralEntityId` عندما يكون النوع `Personal`.
- التحقق من تكرار رقم الهاتف مستقل عن عقد الوسيط ولا يغيّر حقوله.
- القيمة `referrerType = 'Other'` تظهر فقط كقيمة أولية تراثية داخل حالة النموذج، وليست قيمة نهائية موثقة في العقد الحالي.

### 2.8 طبقة الواجهة (UI)

هذا المستوى يصف كيف يظهر العقد للمستخدم داخل الشاشة، وما الذي يراه أو لا يراه حسب النوع.

#### 2.8.1 شاشة الإضافة/التعديل
- الموثق هنا هو تبويب `الوسيط` داخل `ClientModal`.
- الحقول الظاهرة دائماً داخل هذا التبويب:
  - `نوع الوسيط`
  - `طريقة التواصل`
  - `اسم الوسيط` أو بديله المقفل حسب النوع
  - `ملاحظات الوسيط`
- عند وجود `fromCandidate`:
  - `نوع الوسيط` و`طريقة التواصل` يصبحان مقفولين.
  - `اسم الوسيط` يظهر كقيمة مقفولة أيضاً.
  - المستخدم لا يغيّر عقد الوسيط يدوياً، بل يستكمل الحقول غير المنقولة فقط.

#### 2.8.2 الشاشات المرتبطة الأخرى
- `packages/web/src/pages/Clients.tsx` يعرض `referrerType` و`referrerName` كحقول قائمة/جدول، ويحتفظ بالقيمة التراثية `Other` في العرض فقط.
- `packages/web/src/components/employees/EmployeeFormModal.tsx` و`packages/web/src/pages/jobs/ManualApplicationEntry.tsx` يعيدان استخدام نفس القيم الكانونية للنوع وطريقة التواصل، لكن كل شاشة تفرض قيودها الخاصة على الحقول الظاهرة.

### 2.9 قيود الاختيار حسب نوع الوسيط

#### 2.9.1 `Personal`
- يظهر `اسم الوسيط` كحقل مقفل ومعبأ تلقائياً باسم المستخدم الحالي.
- لا يظهر حقل رقم وظيفي.
- لا يظهر بحث الزبون.
- `referralEntityId` يجب أن يبقى فارغاً.
- هذا النوع هو الافتراضي عند فتح النموذج الجديد في `ClientModal`.

#### 2.9.2 `Unknown`
- يظهر `اسم الوسيط` كحقل مقفل ومعبأ بالقيمة `مجهول`.
- لا يظهر حقل رقم وظيفي.
- لا يظهر بحث الزبون.
- `referralEntityId` يجب أن يبقى فارغاً.

#### 2.9.3 `Employee`
- يظهر حقل `الرقم الوظيفي` وزر `اعتماد`.
- اسم الوسيط لا يُعتمد من الإدخال الحر؛ بل من نتيجة lookup للموظف.
- عند نجاح lookup، يُعرض اسم الموظف ويُحفظ كـ `referrerName`.
- `referralEntityId` يجب أن يساوي معرف الموظف المختار.
- إذا فشل lookup، يجب عرض خطأ واضح وعدم السماح بإتمام اعتماد الوسيط كموظف.

#### 2.9.4 `Client`
- يظهر حقل البحث عن الزبون الوسيط.
- الاختيار يتم من قائمة اقتراحات، وليس من إدخال حر فقط.
- اسم الوسيط يُثبت من الزبون المختار، ويُحفظ كـ snapshot.
- `referralEntityId` يجب أن يساوي معرف الزبون المختار.
- إذا لم يوجد زبائن متاحون ضمن الصلاحيات، يجب إظهار رسالة تعطيل واضحة.

#### 2.9.5 القاعدة المشتركة بين كل الأنواع
- `sourceChannel` مطلوب تشغيلياً في كل الأنواع.
- `referralNotes` دائماً قابل للتحرير.
- لا يجوز أن يبقى حقل الاسم فارغاً عند الحفظ إذا كان النوع أحد الأنواع الأربعة الفعلية.
- أي قيمة تراثية خارج العقد الحالي يجب أن تُوثّق كاستثناء أو كقيمة انتقالية، لا كخيار رسمي.

### 2.10 مراجعة الشاشات وتقييم المطابقة

> التقييم هنا يخص **مطابقة عقد الوسيط** فقط، لا جودة التصميم العامة.

#### 2.10.1 `packages/web/src/components/candidates/CreateReferralSessionModal.tsx` — **93/100**
- الأقرب للعقد المرجعي في القيم والقيود.
- يفرض الأنواع الأربعة الموثقة فقط.
- يضبط `sourceChannel` على نفس القيم الكانونية.
- يطبّق lookup للموظف/الزبون ويمنع الحفظ عند غياب الوسيط المطلوب.
- النقص الوحيد تقريباً أنه شاشة مستقلة لإنشاء ورقة إحالة، وليست هي نفس عقد الزبون المباشر.

#### 2.10.2 `packages/web/src/components/ClientModal.tsx` — **88/100**
- شاشة الزبون الأساسية وهي المرجع الأهم عملياً.
- فيها قفل واضح في وضع `fromCandidate`، وlookup صحيح للموظف والزبون.
- تستخدم القيم الكانونية لـ `referrerType` و`sourceChannel`.
- الخصم الأساسي: وجود قيمة تراثية `Other` كتهيئة داخل الحالة، مع مسار تراثي لـ `App` في التطبيع.

#### 2.10.3 `packages/web/src/pages/jobs/ManualApplicationEntry.tsx` — **91/100**
- عقد واضح ومرتب للوسيط ضمن إدخال الطلب اليدوي.
- الأنواع والقنوات مطابقة للعقد الكانوني.
- التحقق أثناء الحفظ قوي وواضح، والـ lookup منظم.
- الخصم البسيط: الشاشة أوسع من عقد الوسيط نفسه، لأن الوسيط فيها جزء من سير إدخال الطلب الكامل.

#### 2.10.4 `packages/web/src/components/employees/EmployeeFormModal.tsx` — **84/100**
- يعيد استخدام نفس القيم الأساسية ويطبق lookup للموظف والزبون.
- ممتاز من جهة التطبيع العملي لقيمة الوسيط.
- الخصومات:
  - يسمح بترك النوع فارغاً في البداية.
  - يتعامل مع الوسيط كجزء من عقد موظف أوسع، لا كعقد مستقل.
  - يحتوي تطبيعاً تراثياً لـ `App` أيضاً.

#### 2.10.5 `packages/web/src/pages/Clients.tsx` — **72/100**
- يعرض `referrerType` و`referrerName` بوضوح في الجدول.
- يفيد في التحقق البصري من البيانات المخزنة.
- الخصم الأساسي: يقدّم `Other` كخيار عرض، وهذا يُبقي أثراً تراثياً ظاهراً في الواجهة.
- هذه الشاشة قراءة/فلترة أكثر من كونها تطبيقاً للعقد.

#### 2.10.6 `packages/web/src/pages/ClientProfile.tsx` — **69/100**
- يعرض نوع الوسيط والاسم ومصدره ضمن الصفحة التفصيلية.
- جيد كمرآة قراءة للحالة المخزنة.
- الخصومات:
  - يحمل `Other` في خريطة العرض.
  - لا يفرض قيوداً تشغيلية، لأنه شاشة عرض لا شاشة إدخال.

#### 2.10.7 `packages/web/src/components/candidates/SessionDetailsModal.tsx` — **80/100**
- يعكس الوسيط من بيانات المرشح إلى نموذج عرض منظّم.
- جيد في نقل الاسم والمصدر والنوع والـ `referralEntityId`.
- الخصم: قراءة فقط، ولا يشارك في فرض القيود أو التحقق.

#### 2.10.8 `packages/web/src/pages/EmployeeDetail.tsx` — **82/100**
- يعرض الوسيط ومصدره وملاحظاته بشكل مباشر وواضح.
- جيد في القراءة والتتبع التاريخي.
- الخصم: لا يطبّق التحقق، وهو تابع لدومين الموظف وليس شاشة الوسيط الأساسية.

### 2.11 الخلاصة التنفيذية لعقد الوسيط

- العقد التشغيلي صار واضحاً على ثلاث طبقات: البيانات، الواجهة، والقيود حسب النوع.
- أفضل شاشتين من ناحية المطابقة الآن هما:
  - `CreateReferralSessionModal.tsx`
  - `ManualApplicationEntry.tsx`
- أكثر شاشة تحتاج تنظيفاً من بقايا التراث هي:
  - `Clients.tsx`
  - ثم `ClientModal.tsx` بسبب `Other` و`App`.

### 2.12 قائمة التوحيد الدقيقة المطلوبة

> هذه القائمة هي مرجع التوحيد العملي بعد التوثيق، ومقصود بها ما يجب أن يبقى متطابقاً في كل الشاشات والـ payloads.

#### 2.12.1 القيم الكانونية الثابتة
- `referrerType` الرسمي: `Personal`، `Employee`، `Client`، `Unknown`.
- `sourceChannel` الرسمي: `Acquaintance`، `PhoneCall`، `SocialMedia`، `Campaign`.
- `App` لا يُعامل كقيمة رسمية في الواجهة؛ هو alias تراثي فقط ويجب أن يطابق `SocialMedia`.
- `Other` لا يُعرض كخيار إنشاء رسمي؛ إذا ظهر في البيانات التاريخية فيبقى كحالة تراثية فقط.

#### 2.12.2 الحقول التي يجب أن تتطابق في كل شاشات الإدخال
- `referrerType`
- `sourceChannel`
- `referrerName`
- `referralEntityId`
- `referralNotes`
- في الشاشات التي تدعم lookup، يجب أن يبقى `referrerName` ناتج lookup أو snapshot، لا إدخالاً حراً عندما يكون النوع `Employee` أو `Client`.

#### 2.12.3 قاعدة النوع `Personal`
- الاسم يُملأ تلقائياً باسم المستخدم الحالي.
- `referralEntityId` يجب أن يكون `null`.
- لا يظهر lookup للموظف أو الزبون.
- يجب ألا تسمح أي شاشة بحفظ اسم يدوي مختلف عن اسم المستخدم في هذا النوع.

#### 2.12.4 قاعدة النوع `Unknown`
- الاسم يُملأ بالقيمة `مجهول`.
- `referralEntityId` يجب أن يكون `null`.
- لا يظهر lookup للموظف أو الزبون.
- لا يُعامل هذا النوع كخيار حر للاسم.

#### 2.12.5 قاعدة النوع `Employee`
- يجب إدخال رقم الموظف أو البحث عنه أولاً.
- عند النجاح، يُثبت الاسم من سجل الموظف.
- `referralEntityId` يجب أن يساوي `employee.id`.
- إذا فشل lookup، يجب أن يظهر خطأ واضح، ولا يُسمح بالحفظ كوسيط موظف.

#### 2.12.6 قاعدة النوع `Client`
- يجب الاختيار من نتائج البحث، لا إدخال الاسم يدوياً فقط.
- الاسم الناتج يجب أن يكون snapshot للزبون المختار.
- `referralEntityId` يجب أن يساوي `client.id`.
- إذا لم توجد نتائج متاحة ضمن الصلاحيات، يجب أن تكون الرسالة صريحة وليست عامة.

#### 2.12.7 قاعدة `fromCandidate`
- عند نقل البيانات من المرشح، تُقفل حقول الوسيط المنقولة.
- لا يجوز تعديل `referrerType` أو `sourceChannel` أو `referrerName` من هذا المسار يدوياً.
- يجب أن يبقى `referralEntityId` كما هو من المصدر إذا كان موجوداً.

#### 2.12.8 شاشات العرض فقط
- `Clients.tsx` و`ClientProfile.tsx` و`EmployeeDetail.tsx` و`SessionDetailsModal.tsx` يجب أن تعرض نفس القيم الكانونية التي يُحفظ بها السجل.
- أي خريطة عرض يجب أن تُبقي التراث ظاهراً فقط كقراءة تاريخية، لا كخيار إدخال رسمي.
- أي شاشة عرض لا تُعتبر مرجعاً لتشريع قيمة جديدة.

#### 2.12.9 إشارات التنظيف القادمة
- إزالة/تقليل أثر `Other` من العروض حيث يمكن ذلك.
- إبقاء تطبيع `App` محصوراً بطبقة التوافق الخلفي فقط.
- توحيد نصوص الخطأ في lookup بين الشاشات بحيث لا تتعدد الصيغ بلا حاجة.
- مطابقة تبويب الوسيط في `ClientModal` مع نفس شروط الإلزام التي تعمل بها بقية النماذج.

## 3. العقد التقني

### 3.1 حدود التوثيق التقني

هذا الملف وثيقة مرجعية تشغيلية، وليس عقداً برمجياً نهائياً. لكنه يحدد بوضوح ما يجب أن يفهمه النظام عند بناء الشاشات أو الحالات أو الإيصالات أو المتابعات.

### 3.2 الكيانات المفهومية

يمكن قراءة الدومين على شكل الكيانات التالية:
- زبون
- مهمة زيارة
- جهاز
- عقد شراء
- تسليم
- تركيب
- تشغيل
- صيانة دورية
- صيانة طارئة
- تحصيل
- هدية
- قطعة مشتراة
- سحب للشركة
- إصلاح بالشركة
- إعادة جهاز
- كفالة
- توقيف مؤقت
- نقل بين العناوين

### 3.3 التحقق المتوقع

عند تحويل هذا التوثيق إلى نظام عملي، يجب التحقق من:
- وجود حالة نهائية لكل مهمة
- حفظ التواريخ والمبالغ والقطع المرتبطة بالمهمة
- فصل الصيانة الدورية عن الطارئة
- بدء العد الزمني للصيانة فقط بعد التشغيل
- الحفاظ على أثر المهام السابقة عند النقل أو الإلغاء أو إعادة التفعيل

### 3.4 ملاحظات عدم التوسّع

- لا يحدد هذا الملف شكل الواجهة النهائية.
- لا يفرض أسماء الجداول أو المسارات البرمجية.
- لا يقرر طريقة احتساب الأسعار أو الغرامات التفصيلية.
- يثبت فقط السلوك التشغيلي المطلوب من منظور العمل.
