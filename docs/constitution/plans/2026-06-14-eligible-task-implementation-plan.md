# خطة تنفيذ DEC-009 — «المهمة المؤهلة» ودورة حياة جهة الاتصال

> **التاريخ:** 2026-06-14
> **القرار المرجعي:** [`decisions/DEC-009-eligible-task-and-contact-lifecycle.md`](../decisions/DEC-009-eligible-task-and-contact-lifecycle.md)
> **الحالة:** مخطَّطة — لم يبدأ التنفيذ
> **الأساس:** تقييم الكود الحالي (يونيو 2026)

---

## 0. تقييم الكود الحالي — أين نحن

| الاستعلام | الملف:الدالة | الفرع | حظر؟ | مرساة N | الملكية | الموقع |
|---|---|---|---|---|---|---|
| #1 العدّ | `planningMarketingTargets.ts:getPlanningMarketingTargets` (≈353) | ❌ `c.branch_id` | ✅ كامل | `CURRENT_DATE` | بالإسناد | حي فقط (مستوى المهمة) |
| #2 الإسناد | `planningMarketingTargets.ts:getPlanningWorkScope` (≈813) | ✅ `ot.branch_id` | ❌ ناقص | `CURRENT_DATE` | lifecycle | حي فقط |
| #3 الداشبورد | `planningMarketingTargets.ts:getAssignedLeadsForTeam` (≈1042) | ✅ `ot.branch_id` | ❌ ناقص | `CURRENT_DATE` | lifecycle | حي فقط |

**باني الأهلية المشترك:** `buildOpenTaskEligibilityPredicate` (≈236) — يغطّي الحالة + نافذة N + `is_active` فقط، ويستخدم `CURRENT_DATE` ثابتاً.
**التسوية:** `assignedTasks.ts:syncAssignedTasks` (إضافة/تحرير، idempotent).
**التوليد:** `telemarketing.ts:/task-lists/generate-from-plan` (≈1162) دمج ذكي يحفظ التقدّم؛ `/task-lists/upsert` (≈1043) **حذف أعمى خطير**.
**جدول الربط مهمة↔جهة:** `contact_target_open_tasks` (link_status: ready/queued/closed/excluded).

**السبب الجذري للانحراف:** ثلاثة استعلامات تكرّر شروط الأهلية يدوياً بدل مصدر واحد.

---

## 0.1 نتائج التدقيق (2026-06-14) — منجز مقابل مطلوب فعلاً

**الاستعلامات الثلاثة كلها حيّة** (لا كود ميت): `planning.ts:338` (#1)، `workScopes.ts:208/323` + `routeAssignments` sync (#2)، `telemarketing.ts:1187` (#3).

**موجود — لا يُعاد بناؤه، يُعاد استخدامه:**
- **آلية الاستثناء كاملة:** `openTasks.ts:/:id/exclude` (2974) + `/restore` (3078) + `/bulk-exclude` (3133). تضبط `excluded_for_date` وتُرجِع `assigned → last_waiting_status`. **المطلوب: حارس فقط** (رفض الاستثناء بعد `in_scheduling`).
- **نمط تحرير المهام عند الإغلاق:** `contactTargetsCleanupJob.ts:80` يُرجِع `open_tasks (assigned/in_scheduling) → last_waiting_status`. **يُعاد استخدامه** لإغلاق الفارغة في نفس اليوم.
- **حبيبة + حالة `contact_targets`:** الجدول مفاتيحه `(branch, target_type='client', target_id, date, team_key, work_location_geo_unit_id)`، و#3 يكشف `ct.status` + `ct.latest_call_outcome`. **طبقة بيانات الداشبورد شبه جاهزة.**
- **الدمج الذكي:** `generate-from-plan` (1162) يحفظ التقدّم + FCFS + يربط `contact_target_open_tasks`. **يصير المسار الوحيد.**
- **قائمة الـ zone متعددة المستويات:** `buildZoneIds` (309) يقرأ `route_points.geo_unit_id` بأي مستوى دون فلترة. **لا تُلمس.**
- **`geo_units` فيه `level (1-4)` + `parent_id`:** النزول للناحية (level 3) من الحي (level 4) ممكن عبر `parent_id`.

**تصحيحات نطاق (تضييق بعد التدقيق):**
- **المرحلة 2:** شروط الحظر تنقص في **#2 (`getPlanningWorkScope`) فقط**. #1 يملكها (715-722)؛ #3 لا يفلتر بل يقرأ المُسنَد (`status='assigned'`, 1128) فيرث الصحّة. → التعديل في #2 حصراً.
- **المرحلة 5:** الناقص هو **`effective_zone` فقط** (يستخدم `c.neighborhood`/`inst.installation_geo_unit_id` بلا COALESCE للناحية) + استعلام «خارج النطاق». `buildZoneIds` لا يُلمس.
- **المرحلة 6:** cron يُغلق **الأيام الماضية فقط** (`date < CURRENT_DATE`)؛ إغلاق الفارغة **في نفس اليوم** غير موجود — يُبنى بإعادة استخدام نمط التحرير في cron.
- **المرحلة 7:** الاستثناء يُقبل حالياً على `in_scheduling` (لا حارس)؛ و`store.generateTaskList` (`useTelemarketingStore.ts:64`) يستدعي `upsertTaskList` الأعمى — **كلاهما خطر مؤكَّد**.
- **المرحلة 9:** طبقة البيانات جاهزة (ct.status مكشوف)؛ العمل واجهة فقط.

**ازدواج محتمل مكتشَف (للتحقق):** نافذة N معرَّفة مرتين — ثابت كود `openTasks.ts:29` (device_demo:7) و`task_type_config.planning_window_days`. استعلامات الأهلية تستخدم `task_type_config`؛ الثابت يخدم فلتر `hideFutureTasks` (1473). **يجب توحيد المصدر أو توثيق الفصل.**

---

## المرحلة 0 (ميتا) — مصدر أهلية واحد مشترك — 🔄 قيد التنفيذ
**الهدف:** منع تكرار الانحراف. توسيع `buildOpenTaskEligibilityPredicate` ليصير **المصدر الوحيد** لكل شروط الأهلية الـ 9 (الفرع، الحظر، N على D، الملكية، الموقع)، واستدعاؤه من الاستعلامات الثلاثة.
- ✅ **منجز (حجر الأساس):** المسند صار يأخذ مرساة D صراحةً (راجع المرحلة 3). الباقي (الفرع/الحظر/الملكية/الموقع) يُنقَل تدريجياً ضمن المراحل 1/2/4/5.
- ملف: `packages/api/services/planningMarketingTargets.ts`.
- مخرَج: دالة/دوال SQL builder بمعاملات موحّدة (`$branchId`, `$zoneIds`, `$actorHrUserIds`, `$D`).
- **تحقّق:** اختبار يثبت أن الاستعلامات الثلاثة تُرجِع **نفس مجموعة الزبائن/المهام** لنفس المدخلات.

---

## المرحلة 1 — توحيد الفرع (لبنة 1 / R-1)
- **التغيير:** في `getPlanningMarketingTargets` استبدال `c.branch_id = $1` بـ `ot.branch_id = $1` (≈719).
- التأكد أن #2/#3 يستخدمان `ot.branch_id` (مؤكَّد فعلاً).
- **تحقّق مسبق (حاسم):** أن إنشاء `device_demo` يضع `branch_id = فرع الزبون`، ومهام الجهاز تضع فرع الخدمة. مصدر البذر: `auto_create_installed_device_for` (migration 282 — يبذر من `COALESCE(service_branch_id, branch_id)`).
- **خطر:** يتغيّر عدّ المدير في حالات تعدّد الفروع — وهذا **التصحيح** المقصود.

---

## المرحلة 2 — إدخال شروط الحظر للإسناد (لبنة 3 / R-4) — ✅ منجز (2026-06-14)
> **مصحّح بعد التدقيق:** التعديل في **#2 (`getPlanningWorkScope`) فقط**. #1 يملك الشروط، و#3 يرث الصحّة من الإسناد (لا يفلتر).
- ✅ **منجز في `getPlanningWorkScope` (بعد `c.deleted_at IS NULL`):**
  - `c.do_not_contact = FALSE`
  - `(c.cooldown_until IS NULL OR c.cooldown_until < $4::date)` ← مرساة D
  - `NOT EXISTS` للزيارة المعلّقة (`vt_uf`/`fv_uf`, `scheduled_date < $4::date`) — مطابق لـ #1.
- ✅ **توحيد cooldown في #1 على D:** الموضعان (العدّ 514 + leadRows 722) `< CURRENT_DATE` → `< $4::date`. (يُكمل متبقّي المرحلة 3.)
- **تحقّق:** فحص أنواع نظيف + SQL صالح + الأعمدة موجودة (smoke test).
- ⏳ **تنظيف لمرة واحدة (متبقٍّ):** مهام مُسندة حالياً لزبائن محظورين → تُحرَّر عند أول `sync` (يُعاد استخدام نمط `contactTargetsCleanupJob.ts:80`). يُنفَّذ عند توفّر بيانات.
- **خطر:** تقلّ المهام المسندة — مقصود.

---

## المرحلة 3 — مرساة D الوحيدة (لبنة 2 / R-3) — ✅ جزئياً (2026-06-14)
- ✅ **منجز:** `buildOpenTaskEligibilityPredicate` صار يأخذ `dateExpr` (افتراضي `CURRENT_DATE`)؛ الـ 3 مواضع `CURRENT_DATE` داخله صارت `${dateExpr}`؛ مواضع `planningMarketingTargets` الخمسة تمرّر `'$4::date'` (=D). `zoneStudy` يبقى على الافتراضي (snapshot يومه). **تحقّق:** SQL صالح + المعامل يثري المسند (smoke test).
- ✅ **منجز ضمن المرحلة 2:** cooldown موحَّد على D في #1 و#2؛ و`unfinished_visit` أُضيف لـ #2.
- **خطر:** يتغيّر ما يُعتبر مؤهلاً عند التخطيط لأيام قادمة — **مطلوب**.

---

## المرحلة 4 — توحيد نموذج الملكية (لبنة 4 / R-2) — ✅ منجز (2026-06-14)
- ✅ **منجز:** الدالة القديمة `buildTeamOwnedClientScopePredicate` (بالإسناد فقط) استُبدلت بـ `buildOwnershipScopePredicate` (نموذج lifecycle: `OP/FOP → شركة` رغم أي إسناد شخصي؛ أو لا مالك نشط → شركة؛ أو مُسند لعضو الفريق). استُعملت في #1 (موضعا العدّ+leadRows) و#2 (حلّ محلّ الكتلة المضمَّنة) → **مصدر ملكية واحد**.
- **تحقّق:** SQL صالح (dev فيه زبائن ضمن النطاق). #1 صار يطابق #2: زبون OP/FOP بمالك شخصي يظهر الآن لكل الفرق (كان يظهر لفريقه فقط في #1).
- ℹ️ `zoneStudy.ts` يبقى بمنطقه الخاص (سطح تحليلي منفصل، DEC-008) — خارج هذا التوحيد.

---

## المرحلة 4.5 — بوابة القسم (لبنة 10 / R-10) — ✅ منجز في #2 (2026-06-14)
- ✅ **منجز في `getPlanningWorkScope`:** دالة `resolveSupervisorDeviceScope(supervisorHrUserId)` تُرجِع `{restricted, modelIds}` (قسم فارغ → `restricted=false` = كل الفرع). أُضيفت معاملات `$7=restricted`, `$8=modelIds`، وشرط WHERE: `(client-basis OR $7=false OR inst.device_model_id IS NULL OR inst.device_model_id = ANY($8))`.
- **مهام الزبون** (`device_demo`) خارج البوابة؛ جهاز بلا موديل لا يُسقَط (R-8).
- **تحقّق:** فحص أنواع نظيف + SQL صالح + جدول حقيقة مطابق (قسم مقيَّد يستبعد الموديل الخارجي؛ قسم فارغ يقبل الكل) + أعمدة `departments.device_model_ids` / `installed_devices.device_model_id` مؤكَّدة.
- ⏳ **متبقٍّ:** تطبيق نفس البوابة على #1 (العدّ) لتطابق العدّ مع الإسناد — يُحزَم مع تغييرات #1 المؤجَّلة (الفرع).
- ℹ️ **ملاحظة:** fallback التخطيط (قسم فارغ = كل الفرع) يخالف عمداً `deviceScopeService` (قسم فارغ = لا شيء) — موثَّق في DEC-009.

---

## المرحلة 5 — الموقع بأعمق مستوى + إظهار ناقصي الحي (لبنة 5) — ✅ جزئياً (2026-06-14)
> ✅ **منجز:** `effective_zone` ومطابقة الـ zone في #1/#2/#3 صارت `COALESCE(c.neighborhood, c.district)` (مهام الزبون)؛ الأجهزة تبقى level 4. التوليد يقرأ `effectiveZoneId` فيرث الـ fallback تلقائياً (لا تعديل توليد). `clients.district` (level 3) مؤكَّد. جدول حقيقة مطابق: صاحب الحي يطابق حيّه (لا سحب زائد)، صاحب الناحية فقط يطابق ناحيته، بلا‑جغرافيا = لا مطابقة.
> ⏳ **متبقٍّ:** «إظهار خارج النطاق» (المهام بلا موقع مطابق تُعرَض للمدير — R-8) = ميزة عرض/endpoint منفصلة.
> ℹ️ أثناء التنفيذ صُحِّح بقايا ملكية #1 leadRows (المرحلة 4 اكتملت للاستعلامين).

### (تفاصيل سابقة)
- **التغيير 1:** حساب `effective_zone` يصير `COALESCE(deepest_available)`: للمهمة client-basis = `COALESCE(c.neighborhood, c.<ناحية>)`؛ device-basis = `COALESCE(installation_geo_unit_id, <ناحية الجهاز>)`.
  - **يحتاج تأكيد schema:** `clients.neighborhood` هو `INTEGER FK → geo_units (level 4)`؛ الوصول للناحية يتطلب أب العقدة عبر `geo_units.parent_id`. تأكيد أعمدة الناحية/السلف.
- **التغيير 2:** ~~`buildZoneIds`~~ **لا يُلمس** — يقرأ `route_points.geo_unit_id` بأي مستوى أصلاً. يبقى التأكد أن واجهة بناء خط السير تسمح بإضافة نقطة ناحية.
- **التغيير 3:** استعلام «خارج النطاق» يعرض للمدير المهام التي لا تطابق أي محطة (CT-G-NULL-LOCATION) — لا ابتلاع صامت.
- **خطر:** متوسط — جوهر التغيير في `effective_zone` (COALESCE للناحية عبر `geo_units.parent_id`). `geo_units` يملك `level`+`parent_id` (مؤكَّد).

---

## المرحلة 6 — تناظر الإزالة في المعاينة (لبنة 7)
- **التغيير:** في جولة تسوية المعاينة (قبل التوليد):
  - جهة فرغت حزمتها بسبب **استثناء يدوي لكل مهامها** → إغلاق بسبب (`closed` + reason).
  - بسبب **خروج طبيعي/إزالة محطة** → تحرير (المهمة لقيد الانتظار عبر `syncAssignedTasks`، والعنصر يُرفع).
- ملفات: `assignedTasks.ts` + منطق جهات الاتصال (`contactTargets.ts` / `contactTargetsCleanupJob.ts` كشبكة أمان فقط).
- **تحقّق:** الإغلاق يسجَّل بسبب صحيح؛ المحرَّر لا يُحسب «مغلقاً».

---

## المرحلة 7 — تجميد الاعتماد + منع المسار الأعمى (لبنة 8 / R-5,R-6) — 🔄 جزئياً (2026-06-14)
> ✅ **7أ منجز:** حارس على `/:id/exclude` و`/bulk-exclude` (openTasks.ts) — يرفض الاستثناء إذا الحالة خارج `(open, needs_follow_up, assigned)` أي بعد التوليد (`in_scheduling`+) بـ `409 task_committed`. فحص أنواع نظيف.
> ⏳ **7ب متبقٍّ:** تحويل `store.generateTaskList → upsertTaskList` (الأعمى) إلى `generateTaskListFromPlan` — يُحزَم مع المرحلة 8/9 لأنه يلمس تدفق التوليد بالواجهة.

### (التفاصيل)
- **التغيير 1:** حارس على `/:id/exclude` + `/bulk-exclude` (موجودان، openTasks.ts:2974/3133): رفض الاستثناء إذا `status = 'in_scheduling'` أو لاحق (مسموح فقط قبل التوليد). حالياً يُقبل بلا حارس.
- **التغيير 2:** تحويل واجهة التوليد من `store.generateTaskList → upsertTaskList` (الأعمى، `useTelemarketingStore.ts:64`) إلى `generateTaskListFromPlan` (الدمج). وتقييد/إزالة `/task-lists/upsert` (الحذف الأعمى، ≈1084) لقائمة نشطة.
- **تحقّق:** محاولة استثناء بعد التوليد تُرفَض؛ إعادة التوليد لا تمسح التقدّم.

---

## المرحلة 8 — نموذج التحديث (لبنة 9 / R-7)
- **إضافة محطة → فوري:** حفظ `route_assignments` يشغّل `syncAssignedTasks` (موجود، `routeAssignments.ts:300`)؛ نضيف تشغيل دمج جهات الاتصال ليظهر الأثر فوراً.
- **إضافة مهمة → يدوي شامل:** زر «تحديث» يعيد `generate-from-plan` (دمج) — يسحب كل مؤهل جديد دفعةً.
- **فحص المغلقة في الدمج:** مغلقة → تُترك؛ غير مغلقة + مؤهلة → تُضاف. (موجود جزئياً ≈1396؛ يُعمَّم ويُختبَر.)
- **تحقّق:** سيناريو مثال DEC-009 لبنة 9 (جهة بمهمة واحدة + مهمة جديدة + تحديث).

---

## تصحيحات كُشفت في اختبار 2026-06-15
- ✅ **خطأ المنطقة الزمنية (toISOString):** `getToday` في 6 صفحات تخطيط كان يحسب UTC فيعرض اليوم السابق بين منتصف الليل و3 فجراً (ظهر 14/6 بدل 15/6). صُحِّح لحساب محلي. ونفس النمط في الـ backend (`openTasks.ts` exclude ×2، `telemarketing.ts` dateToCheck ×2) صُحِّح. (تكرار ~10 نسخ — يُستحسن دالة `getToday()` مشتركة لاحقاً.)
- ✅ **خطأ عرض المحطة (جزء من المرحلة 9):** داشبورد `assigned-tasks` كان يعرض المحطة من `c.neighborhood` (فرع تسجيل الزبون) بدل **موقع العمل الفعّال**. أُضيف `workLocationName` لكل مهمة (جهاز → موقع التركيب؛ زبون → أعمق جغرافيا) ويُستعمل للمحطة. تحقّق: مهمة محمد (جهازه في باب شرقي، مسجَّل في الشيخ بدر) صارت محطتها **باب شرقي** بدل الشيخ بدر. (أكّد أيضاً صحّة فصل الفروع: مهمته فرع 2 وزبونه فرع 3.)

## المرحلة 9 — إعادة بناء الداشبورد على وحدة جهة الاتصال (لبنة 6 / PC-G000)
> **مصحّح بعد التدقيق:** طبقة البيانات جاهزة — #3 يكشف `ct.status` + `ct.latest_call_outcome`، والجدول مجمَّع بالحبيبة. **العمل واجهة فقط.**
- **التغيير:** `packages/web/src/pages/planning/PlanningContactTargets.tsx`: الوحدة الأساسية = جهة الاتصال، المهام تفاصيل متداخلة. المقاييس والفلاتر على `contact_targets.status` (7 حالات) لا على `taskPhase`.
- **خطر:** واجهة فقط، لكن واسع. يأتي **بعد** استقرار طبقة البيانات (المراحل 0-8).

---

## ترتيب التنفيذ المقترح والمخاطر
1. **0 → 1 → 2 → 3 → 4** (طبقة البيانات/الأهلية — الأعلى قيمة والأقل واجهة). كل مرحلة بـ migration/تنظيف عند اللزوم + اختبار تطابق الاستعلامات الثلاثة.
2. **5** (الموقع — يحتاج تنسيق جغرافي، خطر متوسط).
3. **6 → 7 → 8** (دورة الحياة والتحديث).
4. **9** (الداشبورد — أخيراً).

## نقاط تحقّق schema (محدَّثة بعد التدقيق)
- [x] **`geo_units` فيه `level (1-4)` + `parent_id`** — النزول للناحية ممكن. ✅
- [x] **`route_points.geo_unit_id` بأي مستوى** — قائمة الـ zone تقبل الناحية أصلاً (`buildZoneIds` لا يفلتر). ✅
- [x] **`contact_targets` مجمَّع بالحبيبة + `status` + `closing_reason`** — متوافق مع الحالات الدستورية. ✅
- [x] **بذر فرع المهام عند الإنشاء** — محسوم: **المهمة تخصّ الزبون حصراً، لا المرشّح** (ثابت R-9). فمهام client-basis (`device_demo`) لها دائماً `branch_id` = فرع تسجيل الزبون؛ ومهام الجهاز تبذر من فرع الخدمة (`contracts.ts:1531`). `candidates.ts` لا ينشئ مهاماً عمداً. ✅
- [x] **`is_candidate` غير ضروري** — المرشّحون بنيوياً بلا مهام (`open_tasks → clients`، جدول `candidates` منفصل). لا يُضاف فلتر `is_candidate` للأهلية (R-9). ✅
- [ ] **ازدواج نافذة N** (ثابت `openTasks.ts:29` مقابل `task_type_config`) — توحيد أو توثيق الفصل.
- [ ] **واجهة بناء خط السير** — هل تسمح بإضافة نقطة على مستوى الناحية؟ (لبنة 5).
