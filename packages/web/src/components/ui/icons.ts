/**
 * نقطة مركزية واحدة لأيقونات التطبيق (الدرزة / seam).
 *
 * كل الاستيرادات في المشروع يجب أن تكون من هنا وليس من 'lucide-react' مباشرة
 * (بمسار نسبي إلى هذا الملف)، مثال من داخل src/pages/:
 *   import { Search, Plus } from '../components/ui/icons';
 *   import type { LucideIcon } from '../components/ui/icons';
 *
 * الفائدة:
 *  - مكان واحد يتحكم بمجموعة الأيقونات ومصدرها.
 *  - لو أردنا مستقبلاً تبديل المكتبة أو إعادة تسمية/تبديل أيقونات معيّنة،
 *    نفعل ذلك هنا فقط دون لمس مئات الملفات.
 *
 * ملاحظة أداء: lucide-react معلّمة بـ sideEffects:false، لذا تُبقي إعادة
 * التصدير هذه على tree-shaking سليماً — لا تدخل الـ bundle إلا الأيقونات
 * المستوردة فعلاً.
 */
export * from 'lucide-react';
