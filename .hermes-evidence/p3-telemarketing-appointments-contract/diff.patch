# P3 — No code changes in this session.
# This is a contract audit — findings are documented, not fixed.
#
# Proposed changes (NOT applied):
#
# 1. Document the contract explicitly in telemarketing.ts POST /appointments:
#
# diff --git a/packages/api/routes/telemarketing.ts b/packages/api/routes/telemarketing.ts
# --- a/packages/api/routes/telemarketing.ts
# +++ b/packages/api/routes/telemarketing.ts
# @@ -1312,6 +1312,12 @@ router.post('/appointments', requirePermission('telemarketing.appointments.book
#    const appointment = req.body;
#    const createdBy = getCallerId(req);
#    const branchId = getBranchId(req);
# +
# +  // Contract: open_task linkage is OPTIONAL. Booking succeeds without it.
# +  // task_type is NOT validated — any string accepted, falls back to 'device_demo'.
# +  // open_task status transition to 'scheduled' fails silently if status != 'in_contact_list'.
# +  // marketing_visit is NOT created if entity_type !== 'client' or branchId === null.
#
# 2. legacy upsert warning (already has WARNING comment in code — needs open_task_id note):
#
# diff --git a/packages/api/routes/telemarketing.ts b/packages/api/routes/telemarketing.ts
# @@ -797,6 +797,7 @@ router.post('/task-lists/upsert', requirePermission('telemarketing.lists.generate
#  // WARNING: this endpoint deletes and re-inserts all items unconditionally,
#  // which destroys existing call_outcome and status data. It must not be used
#  // by any new code path. It will be removed once all callers migrate to generate-from-plan.
# +// NOTE: items inserted here have open_task_id=NULL — appointments from these items have no task linkage.
